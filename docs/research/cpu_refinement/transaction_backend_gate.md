# GMP-03：自然事务兑现与成本记录

> 2026-09-14；契约 `gmp-contract-v1`，实施基线 `e01cd06`。[小规划](../../plans/cpu_refinement/gmp_03_natural_backend_plan.md)在运行前冻结输入与配额；[执行契约](greedy_multipass_contract.md)、[组合推导](transaction_batch_derivation.md)保持不变。

## 1. 阶段结论与证据范围

**自然网格上已经找到并应用非平凡预算交换批次，无需 Legacy 提供目标 J。** test129 两个快照分别执行 21、23 个交换；Peking 分别执行 0、1 个交换。这个结果支持进入原型准入评估，不证明实际多核收益、持续质量或论文新颖性。

同时出现明确风险：固定当前视图的采样屏幕最大误差未增加，但两个快照的全域高度误差明显上升。当前认证只约束冻结视域样本；不能把这次局部可执行性称为长期 LOD 质量通过，也不能隐藏视图外误差。GMP-04 必须据此收紧原型出口，不回改本次参数寻找更好的结果。

## 2. 输入、覆盖和可复核性

| 项目 | test129 | Peking |
| --- | --- | --- |
| 场景 | `test129-a-b4096` | `peking547-a-b20000` |
| 时刻 | sample14、sample46 | sample14、sample46 |
| 世界尺度 / 高度比例 | 30 / 4 | 80 / 12 |
| 三角形预算 / 深度 | 4096 / 14 | 20000 / 20 |
| 原需求阈值 | 4 px | 0.25 px |
| 公共 k=0 样本 Q | 98,817 | 1,790,881 |

来源为旧 DOD 的 `SplitTopology` 前当前活动叶，已经包含本帧旧合并阶段的来源演化；只把它作为种子网格，不把后续 Legacy 结果当新算法答案。每场景 sample0..46 恢复一次，固定两点导出。捕获前后输入哈希相等，来源完成哈希与无捕获模式相等。

每快照完整建立 Q 的归属、闭面关联和原始需求排序；仅认证全局前 64 个需求，共同回收池为原契约排序的前 64 个内部中心。未处理尾部分别为 2174、2178、19926、19936，**全域 D_need / D_feasible / D_executed 均为未知**。下文比例只针对共同前缀，不可外推总体。

最小角 `atan(1/3)`、β=0.20、η=0.01 px、接收目录、空额度预分配及不回流规则均在自然运行前确定。未增加原语、修改预算、替换快照或补取更有利的需求。没有触发时间/访问配额。

原始产物统一位于 `benchmark-output/cpu-refinement/gmp-03/run-01/`，由 Git 忽略，受版本管理文档保存必要摘要：

- `capture/`：四个中立快照、两份原始 uint16 reference、来源计时；合计 5,548,987 字节 JSON
- `*-final.json`：本报告唯一最终数据源，包含需求身份、失败、预留、oracle、脚本/输入 SHA256；此前 `frontend/receiver/backend` 后缀保留为实现过程，不能混用为最终统计
- `summary.json`、`provenance.json`：摘要、Python/WSL、资产/场景/相机/程序身份和命令；末次仅补注释，重建后二进制 SHA256 不变
- `baseline-v2/`、`baseline-final/`：前后独立进程无捕获来源基准

Ubuntu Python 3.14.4、WSL2；只用标准库，不引入 NumPy 或新运行平台。来源相机使用既有 `benchmark-output/roam-materialization/mpr-01/input-freeze/inputs/camera-samples.csv` 两场景清单。新入口：`parallel_roam_greedy_multipass_snapshot_probe ROOT baseline/capture OUTPUT`；离线入口：`python3 docs/research/cpu_refinement/checks/audit_transaction_backend.py SNAPSHOT OUTPUT`；两者拒绝覆盖已有输出。

## 3. 四层流失与后端能力

| 数量 | test14 | test46 | Peking14 | Peking46 |
| --- | ---: | ---: | ---: | ---: |
| 初始活动面数 N | 4096 | 4095 | 19990 | 20000 |
| D_raw，全域 | 2238 | 2242 | 19990 | 20000 |
| 已检查原始需求 | 64 | 64 | 64 | 64 |
| 接收认证成功 | 45 | 46 | 6 | 1 |
| D_need，前缀 | 45 | 46 | 1 | 1 |
| 三价局部可行 / 交换执行 | 0 / 0 | 0 / 0 | 0 / 0 | 0 / 0 |
| 一般快路径 D_feasible | 45 | 46 | 1 | 1 |
| 一般快路径 D_executed | 21 | 23 | 0 | 1 |
| 无需回收的接收执行 | 0 | 0 | 2 | 0 |
| 总批宽 | 21 | 23 | 2 | 1 |
| 最终活动面数 | 4096 | 4095 | 19994 | 20000 |

一般后端的前缀 `R_local` 均为 1；`R_execution` 分别为 21/45、23/46、0/1、1/1。Peking14 的批宽 2 是两次使用空额度的细分，**不是两次预算交换**；5 份命名空额度中 3 份因冲突闲置，按冻结规则没有回流。唯一需回收需求局部可行，但与已经预留的事务冲突。

四个共同池内均没有三价中心。因此三价对照只表明这个旧限制不能表达本池的回收，不能充当有竞争力的传统 ROAM 基线。一般回收在 test129 解除 21/23 个具体高优先级需求的执行阻塞，需求/中心 ID 保存在逐行结果中；不声称整个一般回收空间或最优选择器的兑现率。

限定 oracle 共检查 53 个失败 pair：4 个为快路径漏解且 oracle 构造的 cavity 与接收方兼容，49 个在指定边界、角度、固定 Q 和 τ 下质量不可行。4 个正例分布在 test129 两个快照各 2 个。**没有用 oracle 替换快路径再运行批次**，因此这些不是额外 D_executed，也不是实测的“快路径加 oracle 兑现数”。未审计 pair、池外中心及接收拟合失败不归入数学不可行。

## 4. 正确性、质量及未覆盖状态

已应用批次通过参数域覆盖、共享边/点、顶点 link、最小角、活动预算、共享高度、全部 Q 覆盖及下一轮需求生成核查；正序/反序的逻辑核心相等。接收高度和新非 dyadic 参数坐标保留有理值，未通过重新吸附到栅格掩盖续接误差。

| 独立量 | test14 | test46 | Peking14 | Peking46 |
| --- | ---: | ---: | ---: | ---: |
| 屏幕最大误差，前后相同 / px | 1.573643 | 1.615260 | 0.496345 | 0.506428 |
| 全域高度最大误差，之前 | 0.078431 | 0.078431 | 0.081263 | 0.081263 |
| 全域高度最大误差，之后 | **1.571242** | 0.078431 | **3.921359** | 0.081263 |
| 下一轮 D_raw | 2248 | 2273 | 19994 | 20000 |

P 的浮点运算顺序与同分身份冻结；V 是本次按固定矩阵/运算确定的 reference 视域样本集合。局部接受在该集合上使用精确有理投影平方复核，不将浮点拟合失败或视域边界判定升级为连续几何证明。全局 sampled maximum 可能被未修改区域固定；局部认证才提供被修改可见样本的证据。

高度上升揭示的是 **当前单视图契约容许的质量退化**：允许旧点共同拟合，但缺少视图外约束和最小修改量的解选择准则。它不是预算/裂缝检查失败，也不是可以忽略的数值噪声。本轮不追调高度域或 η；下一原型必须保留原规则作可重复基线，并在持续视图变化验收前解决或明确拒绝这类状态。尚未测量换视点的误差峰值，不能从高度量直接换算未来像素损失。

有限副本验证了几何、样本和新需求，不等于生产法线、脏区间、持久候选缓存、物理分配器及消费者已实现。GMP-02 的完整续接义务仍是后续实现的硬条件。没有真实多线程提交；反序检查不是数据竞争检测或对所有未来实现的证明。

## 5. 工作量与完整费用

| 工作计数 | test14 | test46 | Peking14 | Peking46 |
| --- | ---: | ---: | ---: | ---: |
| 初次样本定位测试 | 244436 | 244463 | 3594896 | 3594896 |
| 闭面样本贡献 | 139908 | 139811 | 1808400 | 1808400 |
| 接收提案尝试 | 242 | 225 | 374 | 383 |
| 接收精确样本检查 | 5489 | 4864 | 67 | 7 |
| 接收线性约束 | 26917 | 23108 | 425 | 191 |
| 局部样本访问，含接收/回收/oracle | 52045 | 82844 | 78907 | 63746 |
| pair 检查，两后端合计 | 5760 | 5888 | 128 | 128 |
| 回收环访问 | 381 | 399 | 384 | 388 |
| 耳切测试 | 213 | 202 | 215 | 216 |
| 回收/oracle 精确样本检查 | 12348 | 21620 | 22241 | 19277 |
| 预留检查 | 835 | 980 | 43 | 1 |
| donor 复用拒绝 | 361 | 351 | 0 | 0 |
| 读写冲突拒绝 | 453 | 606 | 43 | 0 |
| oracle 三角形测试 / DP 转移 | 67 / 67 | 147 / 147 | 124 / 124 | 100 / 100 |

计数针对尝试而非去重需求；不可把多次拒绝当作更多未兑现需求。组合 `sampleTouches` 已含 `priorReceiverTouches`，不能再加一次接收访问。所有 64 中心的回收证书可缓存重用，但随接收 τ 的 pair 判定仍单列。

Peking 的两个前缀各出现 367 次 `no_screen_samples` 提案失败；test129 分别为 84、82 次。v1 的投影长边紧迫性可使没有当前 V 样本的面排在前面，故“高 P”不等于存在可见误差改善证据。这是需求到认证的流失，不能用 donor 可行性解释；本轮没有向后补取需求替换失败项。

| 时间边界 / 秒 | test14 | test46 | Peking14 | Peking46 |
| --- | ---: | ---: | ---: | ---: |
| 初次完整样本关联与刷新 | 0.371 | 0.360 | 5.571 | 5.258 |
| 接收提案生成 | 0.020 | 0.016 | 0.043 | 0.035 |
| 接收认证，内部计时 | 0.747 | 0.625 | 0.043 | 0.033 |
| 一般回收生成与认证 | 0.695 | 0.767 | 0.669 | 0.771 |
| 一般 pair / 冲突 / 预留 | 0.020 | 0.023 | 0.002 | 0.001 |
| 限定 oracle，额外 | 0.322 | 1.053 | 1.362 | 0.920 |
| 一般批次诊断复制及应用 | 0.007 | 0.007 | 0.033 | 0.029 |
| 一般批次全量核查及下轮刷新 | 0.615 | 0.629 | 8.201 | 7.741 |
| **整个单快照诊断** | **3.001** | **3.688** | **30.453** | **18.044** |

这是 Python、有理算术及独立全量检查的成本，不能称为 CPU 原型帧耗时或提交加速。完整时间还包含加载、归档驱动、全域最大值摘要、反序比较、转换和对象释放；Peking14 对两个后端分别验证应用。原始 `seconds.receiverCertification` 是包含摘要扫描的外层窗口；上表采用 `receiverWork.certificationSeconds`，不把外层窗口全部归因为认证。嵌套计时不能直接相加。来源恢复与文件输出另见下一节。

全域工作已登记：首次几何索引、Q 建立、全域 P 和排序，诊断复制、最终全域重建及摘要。定位按每面格点包围范围枚举，再做闭面检查，并非每面扫描整个 Q。生产目标仍为一次初始化后局部维护 sample ownership；相机变化允许全 Q 投影刷新。**本轮没有高效持久实现，也没有证明这些成本能被 CPU 并行摊薄。**

资源账本的精度边界：记录了 Q/关联/访问数、尝试数及实际文件字节；Python 对象峰值字节、逐次分配、每个派生缓存写、全部事务支持分布没有独立插桩，标记未测，不能声称已取得完整微观 memory/work profile。有限池/提案数量与 θ 导出的 d≤19 限制几何记录，样本证书大小不受该常数界限制。本轮不为补齐每字段计数建设 profiler；后续原型须记录实际容量、受影响记录和样本重分配，不能将其隐藏在“常数提交”中。

## 6. 性能前后核查与过程留痕

| 场景 | 修改前无捕获 / ms | 带捕获总计 / ms | 其中捕获/hash/I/O / ms | 最终无捕获 / ms |
| --- | ---: | ---: | ---: | ---: |
| Peking | 1105.894 | 1366.723 | 261.008 | 1093.916 |
| test129 | 151.483 | 215.705 | 49.432 | 148.643 |

每种模式各一个独立进程，未修改生产代码。最终无捕获较之前约 −1.08% / −1.88%，无退化证据，不解释为提速或做显著性推断。观测成本明确保留，不从完整时间中扣掉后伪称原路径更快。只构建新探针，无全 CTest、双渲染后端或重复压力矩阵。

过程修正：首次相机清单六场景与来源两场景不匹配被既有加载器拒绝，改用已有两场景冻结清单；初次链接遗漏 `PerformanceTimer.cpp` 后补齐。Python 3.14 缺少 ensurepip 且依赖下载超时，改用标准库而非修改系统环境。初始拉伸解析网格违反形状前提，改用等尺度夹具，没有放宽角度检查。后续补齐有理新坐标续接、oracle 与 pair 兼容的分类、细分阶段计时后重新执行四次最终诊断；中间结果保留。

相关解析检查覆盖共享边双重贡献、唯一 owner、正交与变深度透视误差平方（2500、1600）、接收认证、六价平面回收及下轮状态。几何/契约/回收/驱动分文件，旧研究工具只复用纯几何函数，未向旧生产大文件堆入新控制流。

## 7. GMP-03 出口

完成自然来源、有限前缀后端消融、精确局部接受与有限应用/下一轮检查，保留正、负、未知和成本。非平凡交换证据成立，**允许 GMP-04 评估一个受限实验原型**。长期质量、持久局部维护、总体兑现率、发现效率、四类参考和多核收益仍未成立。无需为完成原型前规划继续扩大原语或自然面板。
