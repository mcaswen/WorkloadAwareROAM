# EIP-08 最终架构与验收审查

2026-09-15；[规划](../../plans/experiment_infrastructure/eip_08_acceptance_plan.md)；[事实](../../codebase/experiment_infrastructure/eip_08_acceptance_facts.md)

## Critical

未发现本轮基础设施的关键边界违反。算法默认fit与显式immutable保留；独立reference/质量公式未替换；正常计时不混入截图/全Q质量；源样本/相机/实际mesh身份可追溯。真实坏区保留，不按基础设施通过推导算法通过。

## Major / 验收缺口

真实浏览器不可用，因此整套页面布局和真实交互仍待环境恢复或用户检查。实际截图、联系页、图表和见证已看，播放器事件仅软件单测通过。此项必须保留为未完成，不将Major Plan标为全部视觉签收。

## 已解决的Major：测试文件职责冲突

最终diff发现EIP04的新测试占用了历史ExperimentCameraTests.cpp，造成旧测试覆盖丢失及旧target依赖不匹配。现已恢复原文件，新测试移至ExperimentCameraSequenceTests.cpp，CMake仅新target改源文件。两个受影响目标定向构建/测试通过；此项解决后才完成审查，没有借新fixture替代旧oracle。

## Minor与架构核对

输入/资产/相机协议、平台/CPU驱动、GPU回读、Python运行/归约/质量/作图/报告分开。最长新增专用源码约190行；没有把下载/GIS/编码/统计放进算法，没有共享两后端GPU资源管理。旧入口继续存在，新能力显式开关，CMake依赖单向。报告模板较紧凑，可读性后续按实际需求调整，不在此轮扩Web平台。

发现并修正：中间CSV列名漏检；profile-result产物清单顺序；多线程阶段图标签不区分；重复工作曲线图例；无当前profile时措辞不准确。逻辑修复有定向检查，呈现修复重新查看。开发规范7.3下Peking前后0.56%不构成需追加验证的工程影响。

自动采集、源码/输入归档与正常性能分开，开发快验没有扩成正式矩阵。算法质量残余作为后续研究信息记录，未顺手修改算法。此前误把已生效的逐阶段提交授权记为未授权，导致改动累积。该流程遗漏现按大规划第17节分组补交，后续按阶段闭环即提交。

结论：EIP01～08开发与专项验证完成；Agent实际图像视觉完成；浏览器与用户视觉待验收；算法性能/质量不授予新结论。

## 分组补交核查

Major（已纠正）：用户已有逐阶段提交授权，但本轮未及时执行，且文档误记为未授权。现按EIP-01～08的职责与依赖分为八组，共享应用接线集中在第五组；开发规范明确阶段闭环后先提交，再进入下一阶段。

Minor（已处理）：冻结相机CSV的SHA/FNV依赖原始CRLF字节，普通跨平台文本转换会改变输入身份。补充限定目录的Git属性，65条暂存路线逐项与清单SHA一致；没有修改采样值、相机公式或历史证据。

提交核查覆盖各组暂存内容、Python语法、本地头文件及CMake源文件引用；已有阶段构建、正确性、性能与实际图像证据仍适用，没有为补交重跑矩阵。新相机夹具与原有夹具分别保留，原始输出没有进入提交。

本次只提交本地版本；借用用户既有Git身份作为命令级参数，未改Ubuntu全局配置。真实浏览器布局/交互和用户视觉验收仍待完成，不能用提交完成替代。

归档身份核查补充：旧材质Tex_Terrain_Debug_Diffuse.ppm的Windows工作文件采用CRLF头部，而旧Git对象采用LF，导致新材质清单的文件哈希在新检出环境下不匹配。现按图像二进制冻结实际已使用的文件字节；差异仅为三行PPM头部换行，像素载荷逐字节一致，不改变材质外观或运行行为。未修改历史采集清单。
