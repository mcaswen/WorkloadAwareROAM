# CEI-01 统一观测契约审查

日期：2026-09-16。依据[小规划](../../plans/experiment_infrastructure/cei_01_shared_evidence_plan.md)与[代码事实](../../codebase/experiment_infrastructure/cei_01_shared_evidence_facts.md)。

## Critical

未发现需要阻止当前数据阶段提交的问题。GPU来源代处理位于离线观测层，运行器不做算法判断；CPU更新与GPU命令录制不混成同任务加速。实际捕获N、延迟N和空值的边界清楚。

## Major

CBT无完整计时网格，无法使用CPU逐帧hash验证GPU跨进程等价。已改成有限输入/机会/代次契约并明示未证明范围；不能把该检查状态写为完整算法确定性通过。

已有canyon质量歧义与CBI-03 CPU性能回归均未被本阶段修复，继续保留。分析脚本接通不是放宽评价器或关闭回归的证据；后续报告必须继承这些限制。

## Minor

混合历史目录可能都有名为`timing`的manifest ID。实现仅在报告显示层加来源限定，保留原manifestId，拒绝同一路径重复输入；没有改写历史清单或把重复读取算成独立进程。该补充属于原共享分析职责。

## 验证与规划比对

16项Python检查覆盖CSV篡改/缺列、空N、GPU来源代/预热/去重/矛盾、有效质量、逐点差与维度规则。实际5运行混合analysis完成，CBT暖计算19个有效采样，当前N仍空；8质量帧中2无效保留。9个混合suite配置均由原解析器验证，跨预算逐点配对有实际成功及无效样本证据。

CPU原有分组/工作量与基线相等；读取/哈希543.42→509.00ms，内核0.782→0.763ms，未见新增明显成本。约19µs差值不继续调查，单进程不作统计显著结论。无C++/着色器变化，不重复构建或全量CTest。

共享原语被CBI历史报告复用，没有新增第二份GPU归约。文件归属与大规划一致；下一阶段只在此数据契约上补报告和正式编排，不先运行总体矩阵。

结论：CEI-01数据阶段闭环，可以提交并进入CEI-02；整体实验接入仍未完成。
