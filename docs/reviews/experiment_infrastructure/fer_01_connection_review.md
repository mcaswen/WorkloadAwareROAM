# FER-01 实验配置连接审查

2026-09-15；对应[小规划](../../plans/experiment_infrastructure/fer_01_formal_experiment_plan.md)。本阶段只接通已有策略和冻结实验输入。

## 结果

无 Critical/Major 架构问题。配置类型负责读取，Python/C++均拒绝翻边与非Transactional或可变旧点策略的组合；回放适配只传公共开关。关闭开关保留旧 task identity，开启加入身份，生产默认值不变。独立 `flip-recovery.csv` 记录已有五项翻边计数，不改变历史60列帧协议或预算交换分母。

复用既有进程限制、证据归档、质量评价与模式核对，没有新建线程调度器或修改事务算法。正式采集按预冻结60个独立正常进程及18个视觉运行执行；后续质量从实际视觉网格离线评价，避免重复运行等价采集。

## 定向核查

Windows OpenGL 程序及配置测试目标构建完成；有效翻边配置保留，fit/DOD不合法组合被拒绝。Peking原24机会关闭开关的修改前后网格、预算交换、配对、冲突及输出写入逐帧相同。3..23 CPU均值39.35854/38.49950ms，仅作工程回归筛查。

开启开关完整24机会成功，frame8发生净零恢复。计数和实际网格单独保留供正式采集前核对。当前检查不构成跨算法性能或全轨迹质量结论。

原始目录：`benchmark-output/experiment-infrastructure/fer-01-connection`。首次构建后新增计数输出，再次构建；没有在程序运行时改源码，也没有全量CTest、DX或重复历史质量矩阵。正式实验程序在此后冻结，不混用旧二进制。

## 剩余边界

本轮统计脚本属于实验基础设施；当前只准备协议与收集器，统计报告在采集后实现，不能改变已冻结配置、次序或选择规则。正式实验的3次重复只能提供有限重复性证据，不能用帧数放大样本数。
