# ROAM 目标差分物化理论 Gate 完成自审

> 日期：2026-09-12；源码基线：`69ab803`；对象：[已确认小规划](../../plans/roam_parallelism/target_materialization_gate_plan.md)与[研究报告](../../research/roam_parallelism/target_materialization_gate.md)。
> 同一 Agent 完成自审，不是独立同行评审。Lean 只检查报告第 10 节列出的 12 项声明，不验证整份报告。

## 结论

**本轮理论 Gate 在限定抽象事务内通过。** 完整续接投影已从叶和邻接扩展到内部活动状态、双队列、迟滞/本轮标记、预算与待消费网格差分；给出了直接构造及差分敏感的工作/span 界。它支持讨论下一份原型规划，不自动启动 C++，不宣布算法新颖性或真实加速成立。

## Critical

未发现阻断本次理论报告交付的未处理错误。保留以下必要限定：J 及完整差分已给定；M₀ 包含历史而不从 I₀ 猜测历史；参考是独立目标应用规格；相机、轮次和旧键有效性冻结；输出是声明操作接口下的逻辑状态，非原生 DOD 字节状态。

任意历史同目标不一定产生相同本轮标记，报告用反例说明并限定参考；没有据此删空历史状态或宣布一般物化不可能。原生部分失败的逐步对应仍未验证，不能绕过该边界宣称现有 C++ 可直接调用。

## Major

| 位置 / 风险 | 核查与处理 | 剩余边界 |
| --- | --- | --- |
| 报告 §2–3：任务和状态范围 | 独立参考先删后增，每个差分事件一次；H/K 保持，S/G 按差分取并集。内部关系有显式记录，合并读取至多四个直接子叶接口 | 固定目标应用不等价于 greedy 目标选择；实际生产字段映射需未来原型审计 |
| §4–5：完整续接支持集 | 叶变化在 Δ∪children(Δ)；合并资格读取组成员和直接孩子，受影响组在 d[Δ]∪d[parent(Δ)]；两队列抑制状态也在这些支持集内 | 3k/2k 数值计数和几何层次前提为纸面证明，Lean 没有验证三角形几何 |
| §6.4：旧 Pending 的保留 | 收尾时补明旧/新描述符合成，保留其他未消费条目；删除又出现可相消。不得假设旧 Pending 为空或提前复用消费者引用的槽位 | 原日志消费者需要适配；消费量及完整连续网格搬移成本另计 |
| §7：树、分配与阶段费用 | 不用全量复制或串行堆插入冒充批量维护；计入 k 次索引查询、排序、树回收路径、字长与明确单位分配模型 | 串行分配器不自动满足所列 span；结束帧 H 的全量复制、标记失效和视图评分另计 |
| §7：差分是否省去全部工作 | Γ 只容纳 O(k) 次具体几何/评分函数调用；初始静态缓存、差分认证、目标发现和消费均单列 | 未给出从原始地形到目标的 O(k) 算法；小 k 或高 H 时不保证优势 |
| §8：已有覆盖 | 直接对照 ROAM、两篇并行渐进网格、选择性细化及批量有序集合的原文方法 | 未发现明确覆盖整套 ROAM 续接构造的段落，不等于全球新颖性证明；组成工具多已存在 |
| §10：机械结论大小 | 实际检查叶/合并支持集、合法合并闭合、标记和完整逻辑队列修复等式，共 12 项 | 几何连接、完整模拟、Pending、算法运行及工作/span 未机械验证 |

上述适用限制已进入报告主文和出口，不作为已证原型能力。没有通过额外加入 C++ 或扩大证明库来填补当前声明之外的义务。

## Minor

1. 稳定逻辑身份与物理槽位分开；索引树旋转不意味着网格记录搬移。历史缓存、未消费删除条目均计入 N，不能把 N 总写成当前叶数。
2. 调试强制来源、历史操作统计和哈希不在逻辑投影内，报告明确其可见差异；不能宣称所有渲染模式逐字节相同。
3. Lean 注释采用本语言的块/行注释，已逐处补自然标点；没有使用 C++ 的 XML 文档标签。形式化仅依赖 Lean 自带库。
4. k=0、混合细分/合并、多层变化、边界、同分、最大深度、预算满和过期状态均作解析核查；有限例子不替代一般证明。

## 与规划比对及验证

| 要求 | 实际完成情况 |
| --- | --- |
| 先审计，再证明 | MAT-01 先冻结状态和参考，核对文献；没有先堆叶集合机械引理 |
| 局部网格不是完整状态 | MAT-02 完成叶/边界一般证明，MAT-03 单列内部关系、队列、历史和 Pending 的恢复 |
| 差分及生命周期计费 | 保守总界 O(kH+k log(N+1))+Γ_W；无隐藏全域恢复，公共全量工作与消费明确另计 |
| 非平凡并行区间 | 在明确缓存/字长/分配条件下，H=O(log N)、k=⌈√N⌉ 时 span 为多对数级；不保证有限 CPU 性能 |
| 有界机械检查 | 两份 Lean 源文件，12 项定理编译和公理审计；无新库、平台或额外生产依赖 |
| 文件职责 | 一份研究报告、一份自审；规划和研究入口回填；`Locality → StateModel → Lean`，无生产反向依赖 |
| 非冗余验证 | 只编译本轮文件及公理审计；最后注释修改后核对最终源码版本。未运行旧 Lean、CTest、后端矩阵或自然轨迹 |

工具命令、源码 SHA-256、实际公理输出分类及机械范围保存在报告第 10 节。无运行时代码、构建或配置变化，性能前后对照不适用；形式化工具耗时不冒充算法性能。最终核查覆盖 7 个变更文件、36 个可解析本地引用；UTF-8、公式/代码定界、空白及冲突标记未发现异常。Git 范围恰为规划中的 7 个文件，临时验证产物均被忽略，HEAD 仍为 `69ab803`。

理论交付时产物保留工作区。用户随后确认原型 MPR-01 并授权先提交现有理论与规划；本次封存不含原型代码，不推送远程。临时 `.olean`、审计输入及日志留在已忽略的 `build/roam-materialization-gate/`。
