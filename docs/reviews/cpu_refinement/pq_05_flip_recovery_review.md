# PQ-05 架构与验收审查

2026-09-15。审查对象：[PQ-05规划](../../plans/cpu_refinement/pq_05_flip_recovery_integration_plan.md)、[实现事实](../../codebase/cpu_refinement/pq_05_flip_recovery_facts.md)、[实测结果](../../research/cpu_refinement/pq_05_flip_recovery_results.md)。以下针对本阶段出口，不评价整个论文已完成。

## Critical

没有未解决的本次接入正确性阻塞。净零事务没有伪装成空额度细分，也没有跳过Samples/Mesh统一准备发布。State和公共入口都拒绝不支持的政策组合；SetView不能静默切换。准备失败、旧Version、错误类型/几何、反序和未消费Pending已定向核查。

默认关闭轨迹保持旧B完整证据；C1/C8及平台/核心/视觉对应成立。新分母与原预算交换分母没有混合。形状与硬预算没有为了正例被放宽。

## Major

**当前质量仍不是DOD级别，不能把有限持续改善扩大为完整恢复。** frame15 C约1.997px，DOD约.419px；q2改善一次后保持，没有继续趋近参考。当前视图局部最大值下降仍允许+.051778px的逐点退化。结果、规划出口均明确保留，当前默认开关关闭；此限制不需要在本阶段增加HeightGuard、Fit选值或新原语来掩盖。

**成本证据有限，尾帧成本不能被均值覆盖。** C8暖均值+1.78%，但暖P95+9.23%，frame8新增约6.10ms receiver及1.18ms sample repair；两个相同边的认证、下一帧失败认证都付费。没有建立性能竞争性或开销最优性。只对默认探针历史差异做了规定的一次旧/新复核，未复现退化，不再追加性能矩阵。

这两项是保留的研究/适用范围限制，不是隐瞒的实现缺陷；后续若要扩大质量政策或优化恢复工作，需作为明确后续目标处理。本阶段不自动开启。

## Minor

- 构造拒绝账本保留`flip_flip_...`名称，含义在事实文档解释；未为了字符串美观重跑自然采集。
- 首次构建错误使用输出名而非CMake目标名；离线图像后缀修正。失败日志保留，未影响冻结输入或策略。
- 原窗口捕获接口在接线中直接复用，没有给两个渲染器增加新职责；本次只检验OpenGL，不能写成D3D12翻边视觉验收。
- 完整独立样本/mesh重算只用于夹具，生产没有增加全域验证。混合优先序的审查覆盖真实一次高优先恢复和预算序号推理，不把有限夹具说成全状态证明。

## 文件边界与依赖复核

1. 新`TransactionalFlipRecovery`仅承担有限几何目录与认证，未吸收Planner、State写入、渲染或日志职责。
2. `SetProgressTarget`提取原算法一项纯职责；没有复制Fit或引入第二套进展阈值。PQ-04保留独立oracle表达，生产不依赖experiment。
3. 原Reservation继续拥有全局顺序与资源；原Commit负责目标写入；原Samples/Mesh维护派生状态。没有第二条直接修改面数组的通道。
4. 配置与Stats接线遵守既有适配方向。额外在State初建拒绝非法组合，属于同一生命周期前提的补全，已回填规划文件表。
5. 自然数据编排、质量评价、图形归约位于scripts；追溯仅位于experiment/tests，不进入正常帧计时。公共GUI、Classic/DOD和renderer资源管理未修改。

## 验收记录

新定向夹具及原height-policy夹具通过；后补的公共切换/暂停/Reset夹具通过。读取前已检查开发规范、阶段规划与相关代码事实，完成后逐项对照。C++注释覆盖为`4088/26789=15.3%`，复杂模块原门槛保持；新摘要使用`<summary>`，没有阶段编号或`@brief`。

Agent已查看六帧B/C实际画面比较和frame15两处局部截图，未见新增裂缝/孔洞/翻面；截图检查不替代程序化结构与质量证据。所有截图和raw输出位于忽略目录，进入Git的是代码、脚本及结果/事实/规划/审查文档。

结论：**PQ-05按范围收口，接入和本冻结轨迹的持续改善通过，默认关闭。** 完整质量、一般适用范围、最终性能竞争性继续保持未建立，不跟随本阶段的PASS一起升级。
