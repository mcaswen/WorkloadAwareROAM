# GWR-03 架构审查

> 2026-09-14。索引职责及正确性审查通过，局部维护性能风险保留。

Critical/Major：未发现未处理正确性问题。Key 仍携带稳定身份；删除旧 donor 槽先于添加新 donor，避免负新身份先遍历而被旧删除覆盖。准备独立块、保留旧 Count；发布 move assignment 保证无抛出。独立排序 oracle 与实际 A 轨迹均通过。

Minor/性能：h 个块重排放大了稀疏局部维护，已定向复测并在结果页记录。完整当前轨迹仍有收益，授权范围内保留这一可见取舍；不宣称高频稀疏更新普遍胜出。

模板单文件有独立职责，Samples 只生成资格/key 写集，State 仅暴露现有映射的窄查询，没有反向依赖。诊断可以读取完整前缀，正常路径没有全排序，也没有为了新索引给 A 增加逐 N 重建。查询仍扫描 b 个块首，复杂度已披露。两项相关测试及 24 帧对照足够，不跑现有全集。
