# QPC-04E实施与架构审查

日期：2026-09-17。执行者：同一Agent实施后自审，无独立审查者。依据[小规划](../../plans/cpu_refinement/qpc_04e_quality_contract_optimization_plan.md)、[本轮事实](../../codebase/cpu_refinement/qpc_04e_quality_contract_facts.md)、[结果](../../research/cpu_refinement/qpc_04e_quality_contract_results.md)。前期[文档审查](qpc_04e_derivation_documentation_review.md)保留为当时状态。

## Critical

当前实施未发现未处理Critical。生产质量政策、算法源码、全局请求和原批次Apply均未修改；两个已知坏例没有通过新候选条件。形式化记录没有把一般代数检查扩大为生产或几何证明。

## Major

1. **持续质量未解决，不能把本阶段当生产准入。** Canyon24原14个donor全拒；Sierra95新目标人口全部在旧请求资格之外。报告明确有条件保留而非总PASS。后续接入必须独立规划，未提前进入QPC-05。
2. **独立精确值与显示值必须分离。** 审查时补充精确Ψ上下界字符串；逐项有理数外包成128bit dyadic区间，跨零按unknown处理。double摘要只能显示，不可作为验收证书。增加微小进展未知夹具后重跑离线归约，原始自然采集不重复。
3. **完整支持不能依赖可见提案缓存。** C++导出完整FaceSamples闭支持，Python独立局部枚举核对。接口共享只去重不相加，旧/新共享贡献变化记unknown；71个交换全部通过对应核查。原结构/RW合法性仍由原批准batch继承，不冒充新模型完整证明。

以上前两项属于明确保留的后续边界，第三项已由本轮实现和验证落实，不构成要求继续扩大阶段的理由。

## Minor

- 首次CPU探针构建继承CBT图形宏而缺SDL依赖；使用已有core-only构建配置，结束恢复原app/CBT/tests缓存。没有为实验修改生产registry或渲染依赖。
- Python完整Q夹具最初误算7个点，手动列出六组坐标后确认为8个；修正测试预期，不修改实现或Q契约。
- 初版图高度量程只到1.0，视觉核查发现Composite补丁有1.1323旧残差，已统一扩为1.25，避免裁掉数据。图明确区分空可见支持和零误差。
- 新模块分为C++事实捕获、Python数值、运行编排、归约制图；未把控制器或一般几何验证器复制进新文件。C++采用展开控制流和summary注释，未沿用紧凑生产文件的排版。
- 新增自有C++正文注释40行/有效逻辑238行，排除summary标签后约16.81%；注释说明闭支持、缓存所有权、输入身份及删失边界，没有统一强拆两行或使用阶段编号。
- 数值代数前提已显式保留；未机械建立具体Rat/Real实例。此项不妨碍抽象定理出口，但不能在论文表述中省略。

## 规划核对与验证范围

| 规划项 | 实际交付 |
|---|---|
| QC-01～03最小Lean | 独立4.8.0模块、15引理、五主声明公理列表及SHA |
| QC-04～09过程/反例 | 原纸面推导保留，新增数值对应、位复杂度和自然反例结果 |
| 只读捕获、独立账本 | const入口，完整闭支持；312机会与原历史结果/公开工作一致 |
| 七个冻结批次/六参数 | 全部完成，71交换；空批和失败全部保留，无改prefix/门槛 |
| 有用保留与阻断双检查 | 六组阻断两个坏例；三组达到最低机会门槛，Canyon全拒保留 |
| 测试 | C++专项；12项Python边界；Lean；关闭快验和一次定向复测 |
| 性能异常 | 首轮+15%未隐去；复测未重复，按无稳定版本相关证据关闭，不继续追小差 |
| 图与用户验收 | 两张局部诊断图已查看；用户视觉/阶段验收仍待完成 |

未跑全CTest、双后端、200k、CBT/DOD矩阵；这些都不受只读采集和独立数值模块影响。没有为“测试齐全”重跑历史Lean、旧质量轨迹或平台渲染。

结论：本小阶段实施闭环，可交用户验收。成果支持有限质量政策接入设计，不证明持续修复、运行时可行性或优化空间已耗尽。本轮未提交Git，未启动下一阶段。
