# QPC-04G 实施审查

2026-09-17。对象：[已确认小规划](../../plans/cpu_refinement/qpc_04g_proposal_feasibility_plan.md)、[代码事实](../../codebase/cpu_refinement/qpc_04g_proposal_feasibility_facts.md)、[结果](../../research/cpu_refinement/qpc_04g_proposal_feasibility_results.md)。本审查为实现自审，不冒充独立审稿或用户验收。

## Critical

未发现阻止本轮只读诊断交付的未解决Critical。生产Fit、质量默认值和几何目录未修改；私有见证没有进入Apply；三条完整轨迹及两条必要复核与04F离散行相同。自然可行证据均独立检查核心/float，再经原生逐点认证，未用内域判空冒充无解。

## Major

无本阶段未解决实现性Major。以下研究限制必须继续开放：

- 28个局部正例全部违反旧统一最大值门槛，利用它们需要另行修改提案生成/政策；不能宣称本轮已经修复生产。
- 所有正例局部Emax仍不变，12项只有存储级微小进展。有限正例不证明持续恢复时限、有效工作或同质量性能。
- 8个单点无进展证明只覆盖固定目录、旧高、Q/view/目标；不能推广成一般局部重构不可能。
- 没有验证donor配对或一批同时提交这些高度，不能把目录正例数量当成可执行批宽。

这些边界已在结果页、规划第8节与推导总览同步，不阻止诊断闭环，也不自动批准后续生产改动。

## Minor与已修正项

1. **职责集中。** 初版Observe包含逐项输出和外部认证；已拆成Observe/CaptureProposal/VerifyWitnesses，仍在单一实验组件内，无新框架或反向依赖。
2. **构造失败。** B可能在分辨率前置检查返回，没有新点；输出改为null而非无条件at。源文件写入启用I/O异常；不存在的见证ordinal也明确拒绝。
3. **证明充分条件。** “旧超标样本b=0”不足以证明无进展；已补a=h_old，并增加新连接改变常数项的解析反例。实际8项无进展依据更强的必要单点＋完整Q等值。
4. **首轮unknown。** 保存初轮结果后增加单点证书关闭8项，没有通过扩大数值预算/新增试值来追正例。
5. **旧保守模型。** Fit失败未到observer时available=false，不能显示一个伪造空区间。本次正例全部被旧精确门槛排除，故有理由不复制旧Fit恢复失败后的完整保守系统；该范围收缩已登记。
6. **重复统计。** 只保存几何/view指纹，不含所有owner/cache/新身份输入；报告改用“重复线索”，没有宣布生产缓存正确或可删除多少认证。
7. **图示。** 初图见证星号被区间线遮挡，已调整绘制层级；标签明确是核心安全域，两域只对星号认证。没有把图当持续轨迹视觉验收。

## 架构与验证结论

实验捕获→独立Fraction→显式原生复核的依赖方向符合规划。原生命令计时/峰值、离线费用、原生产阶段时间分开；没有新增profiler、生产缓存或Lean工程。新增C++采用展开控制流与中文summary，注释说明数值、身份和只读约束，没有阶段编号或`@brief`。

定向验证为8个Python解析测试、一个C++实际组件夹具、三条原轨迹捕获、仅两条有正例的原生复核，以及Peking普通前后路径。未改生产源码，复用04F全域质量；不运行全CTest/双后端/新自然矩阵。普通路径前后无有意义的同向退化，离线求解均在配额内。

审查出口：**QPC-04G只读可行性诊断完成，可交用户验收。** 持续质量/新Fit/竞争性能仍未通过；未启动QPC-05；本阶段按用户授权独立提交。
