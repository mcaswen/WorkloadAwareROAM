# TPI-02 实施架构审查

日期：2026-09-15。对照[小规划](../../plans/cpu_refinement/tpi_02_public_adapter_plan.md)与[实现事实](../../codebase/cpu_refinement/tpi_02_public_adapter_facts.md)。

## Critical

无未解决阻塞项。核心不依赖 DOD 或研究入口；DOD 仅服务一次种子。状态拥有者、借用有效期、失败恢复、深度与预算边界有定向证据。

## Major

无未解决架构偏离。公开值类型与 PImpl 隔离重型容器；初建验证是从既有研究验证器单状态部分提取，研究层转调，未形成两套规则，也不进入正常更新。测试分配故障支持没有进入生产链接。

## Minor 与限制

没有用自然空批暖调用宣传交易性能。冷适配仍包含种子构造、全量状态/样本初始化；Peking 当前视图一次种子较稀，后续性能/质量出口必须记录。此前短测波动和本轮 DOD 静止增加均保留，不能声称稳定加速或严格性能等价。

核查并补齐了暂停/失败返回的活动面与样本统计；Consume 后范围构造故障可要求全量恢复。零 drawable 不伪造相机，但旧视图构造器仍为兼容执行 clamp，TPI-03/04 必须先检测真实零尺寸。

本轮检查包含必要源码/测试、前后网格与工作量比较、git diff 和注释覆盖；src 15.9%、DOD/Classic 各 20.0%。允许按已授权大规划进入 TPI-03，不把此审查解释为平台或性能准入。
