# 增量 activation spectrum 理论 Gate 完成审查

> 日期：2026-09-12；基线：`f1f1b1d`。
> 对象：[工作区规划第 6 节](../../plans/roam_threshold/theory_workspace_plan.md)、[Gate 报告](../../research/roam_threshold/incremental_gate.md)；本轮为自审，不是独立同行评审。

## 结论

按用户限定的纸面理论 Gate 完成，出口为 **No-Go**。完整记录条件算法及其已证明的局部界；同时确认尚未提供包含视点评价和证书维护的新局部界，核心机制存在明确已有覆盖。关闭的是本候选研究支线，不是所有增量网格方法。

## Critical

未发现需要修改原 Lean 定理的 Critical 问题。本轮没有新增机器验证声明。

已逐项核对：跨帧 anchor 固定；source 批量写入后再按深度传播；节点值不变时停止通知；禁止事件独立约束；并列值按整组计费；阈值区间枚举覆盖未改值却改变目标身份的事件。报告中的纸面算例覆盖减小和混合更新。

## Major

| 问题 | 核查处理与结论边界 |
| --- | --- |
| 少量 source 改变是否偷设为相机输入条件 | `Tsource` 单列，显式全谱与 oracle 下界分别说明；未从连续性推出稀疏性 |
| 条件传播算法能否处理减小 | 重新比较全部常数个直接输入；不是只保留一个失效的全局 argmax |
| 能否仅凭 `Ω(n)` 最坏例否定局部算法 | 不能。报告保留 `s,k` 小时的局部界，并用公共缩放因子反例说明隐式表示可避开物化下界 |
| 证书和移动阈值是否免费 | 推导统一扰动下的阈值界，计入证书访问、求值、内部事件、索引及参照版本维护；未建立的新界明确保留 |
| 是否把近似已有工作认作完整先例 | 区分精确 saturation 与 conservative bound、VDPM 面数反馈与精确 hard budget；没有声称文献已完整解决任意生产 score 的目标维护 |
| 新颖性结论是否过强 | No-Go 基于当前机制覆盖及局部性证据不足，不将定向检索当作穷尽证明 |

## Minor

原文复核时修正了 Acar 2009 算法位置为 §6.2。论文版本和下载状态保留在 Gate 中，没有把抓取日期当作出版日期。

## 范围与验证

仅新增 Gate 报告与本审查，更新规划和 README 入口。原 4 个 Lean 文件、生产代码、构建配置、实验输入和现有研究定义保持原内容；不复跑此前证明、CTest 或性能矩阵。验证采用公式及计数自审、定向原文阅读、UTF-8/本地链接及 Git 范围核查。

下载 PDF 和提取文本位于已忽略的 `build/roam-threshold-incremental-gate/`。本轮材料经用户认可后获准独立提交；没有为关闭 Gate 新增代码或另起实验平台。
