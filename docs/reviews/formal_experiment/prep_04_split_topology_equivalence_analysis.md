# PREP-04：细分拓扑策略不等价分析

> 日期：2026-09-10\
> 状态：已知不等价已修复，当前两后端全集及规定独立运行全部等价，未发现新反例；自然覆盖和环境验收限制见第 7 节\
> 关联：[PREP-04 小规划](../../plans/formal_experiment/prep_04_minimal_cpu_pairing_plan.md)第 6.4、8.2、10 节，[当前配对事实](../../codebase/formal_experiment/cpu_pass_pairing_contracts.md)

## 1. 结论与范围

第 1～6 节保留修复前的反例、代码事实与决策过程，当前实现状态以第 7 节和修复小规划为准。

FACT：`peking547-a-b20000 / splitTopology / sampleIndex=14 / selectionRank=3` 的串行立即执行与并行辅助执行产生不同的活动拓扑、队列成员、修改集合及最终网格。两者各自通过预算、邻接、裂缝、队列和网格有效性检查，但不满足规划要求的同输入、等价输出配对。

这不是已确认的 PREP-04 性能退化，也不是可忽略的哈希顺序差异。当前阶段不应通过删掉该目标、改用一个线程或放宽结果比较来完成验收。来源冻结输入没有被重选或修改。

## 2. 实际失败与复现

完整目标集运行 `benchmark-output/prep-04/integration-20260910/all-1/` 在首个 Peking 场景的第 14 帧失败。`run-metadata.json` 保留 failed，原始记录为 pending 文件，没有被发布为 `pairing_complete`。同帧早先已测的 merge topology 行也被撤销成功资格，这是来源帧原子确认规则的预期行为；直接发生结果不等价的是 split topology。

共同阶段输入哈希为 `4677929227313376363`，进入阶段时有 19,990 个活动三角形、10 个剩余预算和 10 条此前合并修改。两个动作在配置副本后仍保持相同输入哈希。

| 证据 | 串行立即执行 | 并行辅助，P=2 或 8 |
| --- | --- | --- |
| 阶段正确性检查 | 通过 | 通过 |
| 阶段结果哈希 | `1100364126311268133` | `5598545571842840993` |
| 活动三角形数 | 20,000 | 20,000 |
| 活动叶哈希 | `13984722072624635988` | `15836370938175210932` |
| 最终网格完整验证 | 通过 | 通过 |
| 最终网格结果哈希 | `159806467330165317` | `9468994361173074292` |

两组叶集合共有 19,994 个元素，各有 6 个独有元素。串行独有路径包括 `10650` 和 `140737488378281`，并行结果包含它们的子路径。最终网格哈希也不同，因此不能解释成合法槽位换序。

计时外诊断遍历诊断开/关、P=1/2/8 和两个动作，共 12 个组合。诊断开关不改变结果；P=1 的并行请求回退后与串行一致，P=2/8 的真实辅助执行稳定复现另一结果。日志与叶集合为：

- `benchmark-output/prep-04/topology-diagnosis-20260910.log` 及同名目录。
- `benchmark-output/prep-04/topology-plan-diagnosis-20260910.log` 及同名目录。
- 实际应用单目标复现保存在 `benchmark-output/prep-04/inequivalence-20260910/`，分别记录 OpenGL/D3D12 的 P=8 以及 OpenGL 的 P=1 对照；完成状态以各运行元数据为准。

## 3. 原因证据

FACT：只读计划生成 19,857 个候选，其中安全内部候选只有 2 个，其余 19,855 个为边界候选。按全局优先级排序，前五项均为边界项；两个安全候选分别位于零起始 rank=44 和 rank=353，对应路径 `140737488378281`、`10650`，分配到不同安全块。

FACT：`PlanDataOrientedRoamSplitTopology` 排序后跳过边界候选，使用剩余预算选择安全内部候选。`CommitScoredSplitTopology` 先提交这些候选，随后同步预算并调用 `RunSplitSerialConvergence`。串行立即执行直接进入全局长期队列收敛，不先提交这两个候选。

INFERENCE：空间上安全、互不冲突的提交不等于与全局优先级及有限预算交换语义可交换。提前细分较低优先级节点改变了收敛前的预算、拓扑及可合并集合；后续串行收敛没有保证恢复到纯串行策略的同一固定结果。两条安全候选与实际独有叶之间的对应关系支持这一机制，具体修复仍需分析动态闭合与预算交换，不宜只加一个简单排序条件即宣称证明等价。

FACT：PREP-04 没有修改拓扑计划文件，`RunSplitSerialConvergence` 与实施前正文逐字一致，生产 split commit 路径也保持原逻辑。另将 HEAD `2dac3ea` 的整个 `DataOrientedRoamTopology.cpp` 导出并重新编译，替换诊断程序中的该翻译单元后，仍得到相同输入和上述两个不同结果。日志为 `topology-baseline-diagnosis-20260910.log`。这是旧拓扑实现的定向复现，其他链接组件来自当前构建，不能冒充原应用二进制的完整性能对照。

## 4. 验收影响

Critical：PREP-04 第 8.2 节要求六场景全部冻结目标可靠配对，目前该要求不满足。36 项 CTest 通过仅证明既有夹具范围，不能覆盖或推翻这个实际反例。

测量执行器正确保留前序修改、识别不等价并拒绝发布。正式重复周期、三次短运行稳定性和跨目标胜负解释不应在已知不等价的目标集合上继续声称通过。容量处理与普通生产实现前后性能属于独立核查，可继续执行；它们不能解除本条阻断。

## 5. 修复方向与用户决定

2026-09-10 最新决定：用户暂时关闭独立性能问题，要求先提交已有工作，再开始本拓扑等价性修复。连续前缀方案与文件边界据此进入实施；后续重要设计变更及放宽条件仍依小规划约束处理。

2026-09-10，用户要求新增独立修复小规划，倾向先采用全局优先级的连续安全前缀，遇首个不安全候选后不再提前提交后续项。已形成[细分拓扑修复小规划](../../plans/formal_experiment/prep_04_split_topology_equivalence_fix_plan.md)，先打现有反例，再研究等价放宽条件与实验。拟议职责边界为：

| 文件或模块 | 拟议职责 |
| --- | --- |
| `DataOrientedRoamTopologyPlan.h/.cpp` | 明确可提前提交集合相对于全局优先级、闭合与预算的条件；不能把空间安全当作结果等价证明 |
| `DataOrientedRoamTopology.cpp` | 在既有生产提交与串行收敛流程中执行已确认的条件；不引入实验专用拓扑算法 |
| `DataOrientedRoamWorkloadProbe.cpp` | 若可执行计划语义改变，复用相同计划并重新定义受影响的工作量事实 |
| 现有 DOD 测试与冻结发现/配对集成 | 增加该实际反例、预算临界和动态闭合覆盖；验证来源、完整输出与真实线程派发 |

关键决策须先确认：保留严格输出等价作为 CPU 配对前提；对不能证明可交换的输入是否允许生产策略串行回退；计划规则变化后，受影响的输入/目标版本和冻结发现如何重新生成。回退可能减少可测并行覆盖，应如实交给后续 CPU gate 判断，不能隐藏它来制造交叉信号。

另一选择是暂缓 PREP-04，在重新审查研究问题后讨论不同质量输出是否可比；这会改变已确认的实验契约，不是本阶段局部修正。当前未实施任何生产策略修复，也未改变等价标准。

依据项目 `AGENTS.md` 的“实现中如必须改变已确认的边界、职责、依赖方向或公共接口，应先说明并重新确认”，以及小规划第 10 节的范围约束，生产拓扑语义修复需另行确认。当前实现、失败数据和本分析已形成可审阅结果。

## 6. 规划扫描补充

FACT：`SplitEntryPrecedes` 的同分顺序是 `PathId` 升序，而 `PlanDataOrientedRoamSplitTopology` 与 `FlattenSplitChunks` 使用 `Sequence`。`SnapshotPersistentSplitQueueCandidates` 的 `Sequence` 来自堆数组遍历；它不是稳定路径优先级。方案 A 必须统一真实串行顺序，不能只在原过滤循环中把 `continue` 改为 `break`。

FACT：快照逐项过滤 `ShouldSplitWithScore == false`，而串行收敛遇到这类队首会停止；串行还会优先处理低于合并阈值的有效合并队首。规划因此保留这些控制流边界，不把过滤后的候选向量认作完整决策序列。

INFERENCE：在已知第 14 帧，最高分安全项为 2.41175，而队首不安全项分数约 2.42467，统一同分顺序后仍应得到空前缀。其修复后等价结果尚未执行验证。非空前缀还需检验动态子节点优先级、合并资格、预算和同块失效，详细条件与反证实验已写入小规划。

## 7. 方案 A 的实施与覆盖边界

2026-09-11 收尾更新：用户授权继续 PREP-04 后，当前版本两后端各 113 目标全集、10 个固定首目标名义重复，以及两轮独立短运行共 72 个应用进程全部完成。严格配对、来源与共同目标输入/结果身份核验均通过，没有新不等价反例。下文未完成运行的表述是收尾之前的状态；当前结果见[拓扑规划第 9.8 节](../../plans/formal_experiment/prep_04_split_topology_equivalence_fix_plan.md#98-prep-04-剩余运行结果)。

独立短运行唯一复测后仍有 20/22 个目标/动作不满足 5% 漂移门槛，已停止采样和相关胜负解释。环境限制不改变已知反例的修复结论，用户关闭的工程性能问题保持关闭。自然细分并行覆盖仍为 0/24 个所选目标，放宽条件未启用，不能据当前数据宣称普遍等价或进入 PREP-05 的投入判断。

2026-09-11 当前核对：原 `sampleIndex=14` 已恢复严格结果等价；旧 113 个核心输入、新 OpenGL 113 个目标全集配对通过，后续空前缀优化的专项保持这些关键结果，当前没有观察到新的不等价反例。新发现进一步确认六个 pilot 场景 384 个自然细分前缀全部为空，受控两块正例不能替代自然覆盖。用户已关闭剩余性能问题，下一步收尾不再受该项阻断；未完成的独立运行及放宽条件仍见[拓扑小规划第 9.7 节](../../plans/formal_experiment/prep_04_split_topology_equivalence_fix_plan.md#97-性能关闭后的拓扑状态)。本次只复核已有证据，没有重跑测试或修改结果标准。

FACT：2026-09-10 按已批准边界完成连续前缀实现。统一 `Score`/`PathId` 优先级，并保留真实串行停止项、优先合并与预算边界；首个不安全项之后仍完整分类，但不安排提前提交。主线程整理成功提交记录使用冻结分数，串行收敛正文未变。

FACT：第 14 帧保持原输入哈希、19,990 个活动叶、剩余预算 10 和前序 10 条修改。两条安全后缀仍计入分类，但安排数量为 0；P=1/2/8、诊断开关和 W=0/R=1、W=1/R=6 均恢复原串行阶段结果及完整网格结果。旧 113 个冻结目标在核心入口全部通过输入哈希与结果等价回归，没有重选旧反例或更换预期值。

FACT：受控夹具只提升两个既有安全节点的冻结分数，构成两个安全块的连续前缀；P=2/8、W=5/R=30 均完成真实并行提交并与串行等价，缺池和单块回退也通过。该夹具验证生产执行能力，不属于自然发现样本。两条独立流水线完成第 14 帧后的原轨迹与交替视点对照。

FACT：对原六场景的 384 个自然来源帧扫描，没有发现多块连续前缀。因此当前结果只支持该固定集合上的修复，不能声称非空前缀已获得广泛自然覆盖，更不能据此推断普遍可交换性。

| 放宽条件候选 | 本轮证据与下一步实验 | 当前决定 |
| --- | --- | --- |
| 批次可交换性 | 两个真实安全块的受控评分正例等价；自然多块前缀为 0。还需逐一引入更高优先级新子节点、动态合并与资格失效，验证所有中间状态的读写影响域和后继队列 | 尚无充分条件证明，保持关闭 |
| 多轮连续前缀 | 当前 A 只规划一轮，原轨迹未提供多块覆盖；应先讨论提取单次串行决策接口，再验证每轮预算、结果和额外成本 | 需要接口 Review，不在本轮实施 |
| 越过可跳过项 | 规则测试证明迟滞停止项不能越过，原第 14 帧证明空间安全后缀不能直接提前；后续须区分串行会阻塞后继续和会直接结束的项 | 不扩大资格 |
| 预算充足时跨边界 | 预算 0/1/前缀长度边界规则通过，但预算本身不消除动态优先级和闭合差异；下一步需限定完整闭合集合及合并交换条件 | 不把预算充足当作充分条件 |

进一步证明批次可交换性时，还须纳入串行迭代上限：`RunSplitSerialConvergence` 每次调用重新计数，上限为 `max(1024, TriangleBudget × 8 + Nodes.size() × 4)`。提前提交没有消耗该循环的迭代次数；当前尚未观察到由此产生的反例，但充分条件必须排除两条路径在不同位置触及这一停止边界，或在单独 Review 后统一其语义。

实现与验证日志位于 `benchmark-output/prep-04-topology-fix/`；新的严格发现/配对、普通性能对照与最终审查结果见[小规划第 9 节](../../plans/formal_experiment/prep_04_split_topology_equivalence_fix_plan.md#9-实现情况)。原失败目录保持不变。当前没有进入 PREP-05 或放宽结果等价标准。
