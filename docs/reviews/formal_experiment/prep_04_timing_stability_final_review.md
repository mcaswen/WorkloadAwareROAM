# PREP-04 计时稳定性的最后核查与阶段移交

> 日期：2026-09-11\
> 状态：最后核查结束，保留漂移限制；按用户决定结束 PREP-04 排查并转入 PREP-05\
> 依据：[PREP-04 小规划](../../plans/formal_experiment/prep_04_minimal_cpu_pairing_plan.md)第 7.1、11.9 节、[当前事实](../../codebase/formal_experiment/cpu_pass_pairing_contracts.md)第 13 节\
> 授权：用户要求提交、最后排查；若仍不能解决，则写明文档进入 P5

## 1. 核查范围与结论

本次仅复核现有数据和统计实现，不启动第三轮采样、不重新打开已关闭的工程性能问题。读取 `short`、`short-repeat` 的 60 次独立运行，用单独审计脚本从原始 CSV 重算每个目标/动作的进程内中位数及两轮极差，逐字段对照已有报告。源码提交为 `ec8792e`，对应此前已测的连续前缀和空前缀短路实现；提交不改变归档数据原有的 `gitCommit`、工作区源码摘要或 RunId。

没有发现可通过修正统计或记录消除的错误。最终仍为 20/22 个目标/动作不满足既定 5% 上限。此次检查到此结束，保留 PREP-04 环境验收未通过的事实；用户已允许在写清限制后推进 PREP-05，阶段移交不等于补发环境合格结论。

## 2. 核查事实

| 核查项 | 结果及其含义 |
| --- | --- |
| 原始策略行摘要 | 60 份 `pair-samples.csv` 的字节数和 SHA-256 与各自元数据相同，没有文件被后处理覆盖 |
| 独立统计单位 | 60 个不同 RunId；应用进程起止时间没有交叠，不把重复块算成独立进程 |
| 预热与计量 | 计量行均为 `isWarmup=false`；W=2/R=10/P=8，每动作的绝对块号为 2～11、重复号为 0～9 |
| 来源与分组 | 60 次运行属于同一 OpenGL 执行身份；源码、程序、输入及环境记录前后一致；目标和动作分别计算，没有混合工作量 |
| 轮次 | 每目标/动作恰好六个进程，按真实启动时间分为两组三次；两轮 RunId 与原报告一致 |
| 漂移公式 | 独立重算中位数、绝对极差、相对极差及 `range <= 0.05 × median`，全部字段与原报告一致 |
| 分析排除 | 同执行来源出现不稳定项后排除其胜负解释，是当前 `short_run_stability` / `analyze_attempts` 的明确规则，没有把遗漏短运行的 D3D12 写成环境稳定 |

已有 72 次配对的严格结果、完整来源及标定核验继续复用，没有重跑全量审计、CTest 或普通性能矩阵。最后核查产物为 `benchmark-output/prep-04-topology-fix/final-check-20260911/{check_existing_records.py,audit.json}`；脚本摘要随 JSON 保存，新增测量进程数为 0。

## 3. 原因边界与处置

FACT：当时记录为 Ryzen 9 9950X3D、32 个逻辑处理器全亲和性掩码、普通优先级、平衡电源计划。每次环境快照一致只能证明这些设置没有变化，不能证明每个计时块的实际核心、有效频率或抢占条件一致。

UNCERTAIN：现有记录不包含逐块核心驻留、有效频率和抢占轨迹，不能把漂移确定归因于其中某一项，也不能据此认定生产版本退化。本轮不改系统电源设置、处理器亲和性或优先级，不增加长期跟踪工具。

按用户最新授权，P4 的代码、正确性与记录交付结束，计时漂移保留为 P5 的已知风险。旧分析文件保持排除状态，不删除短运行证据、不修改旧阈值或重新标记旧结果。P5 在新数据采集前明确自己的探索判断契约；是否采用三次配对方向而非旧动作绝对耗时 5% 上限作为投入判断依据，须在 P5 小规划中显式说明。正式实验门槛仍保持原规范。

## 4. 架构与交付核查

### Critical

没有新的结果不等价、输入身份冲突或配对完整性问题。旧第 14 帧及受控实际并行证据保持，未改动生产执行、取证或计时包络。

### Major

环境稳定性仍存在限制，自然细分并行覆盖仍为 0/24 个所选目标。这两项都随 P5 移交，不能推导出“不存在交叉”，也不能支持直接进入上传重构。P5 应允许“证据不足”作为有效结论，禁止通过无限复测追求通过。

### Minor

本轮没有新增生产模块、依赖或正式脚本。独立重算只作为归档审计产物，未复制一套正式统计框架；不需要重新做生产性能基线。源码和文档分开提交，提交信息只描述修改内容，验证证据保存在本审查和阶段规划。
