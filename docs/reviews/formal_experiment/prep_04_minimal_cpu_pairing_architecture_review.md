# PREP-04 最小 CPU 配对与分析架构审查

> 日期：2026-09-10～2026-09-11\
> 状态：工程交付和最后排查结束，保留环境限制；用户批准移交 P5，当前处置见第 7 节\
> 对照规划：[PREP-04 小规划](../../plans/formal_experiment/prep_04_minimal_cpu_pairing_plan.md)\
> 当前事实：[CPU 阶段配对契约](../../codebase/formal_experiment/cpu_pass_pairing_contracts.md)\
> 授权：用户确认按步骤实施；2026-09-10 另行许可提交现有成果，代码提交为 `7522db7`，文档单独归档

## 1. 架构判断

2026-09-11 当前判断：方案 A 及空前缀优化遵循已确认边界，已知反例维持修复；当前版本两后端各 113 目标全集、10 个固定首目标名义重复及两轮独立短运行全部完成，共同目标的输入/结果身份一致。性能问题已由用户关闭，短运行环境检查在唯一复测后仍未达标，已停止采样和相关胜负解释。第 4～5 节按时间保留原失败及处置经过，当前验证以第 6 节和[拓扑专项审查](prep_04_split_topology_equivalence_architecture_review.md)为准。

实现遵循已确认的分层与依赖方向。DOD 内部分别拥有配对顺序、单动作连续计时和停表后证据；正式实验值层不持有算法状态；Benchmark 组织真实场景、目标重建、转换和发布；Python 负责实际执行追溯及离线完整性审计和统计。

单阶段测量直接调用生产 `ExecuteDataOrientedRoamPass`。来源状态同步借用，副本拥有算法容器，线程池与高度图不转移所有权。修改没有创建第二套评分、拓扑事务或网格生成算法，没有调整两个图形后端的上传模型。

原生产策略不等价已在已测范围修复；当前工程架构既核验成功配对，也可靠保留失败与环境限制。六个 pilot 场景自然细分前缀为空，已有受控多块正例不构成广泛自然覆盖或普遍等价证明。

## 2. 与规划和开发规范的比对

| 要求 | 实现及证据 |
| --- | --- |
| 实施前阅读并确认 | 已读开发规范、规划/事实/审查指南、上位规划、PREP-01～03 和阶段协议；用户确认后先采集基线，再修改代码 |
| Reuse / Extend / Wrap / Create | 复用生产执行、输入哈希、工作量计划、验证与 CSV 编解码；扩展 CLI/Runner；包装旧配对入口；按规划创建 Measurement/Evidence、公共输入映射、配对/标定和 Python 分工 |
| 文件与依赖边界 | 值层只依赖基础领域类型；Benchmark 依赖 DOD 与值层；DOD 不反向依赖实验记录、Benchmark 或文件 I/O |
| 连续计时 | 深复制、线程创建/准备、哈希和证据在包络外；生产快照、分块、同步、串行收敛和网格容量变化在单次执行内 |
| 诊断隔离与来源只读 | Pilot 三个诊断开关关闭；来源流水线保留诊断并于帧末确认；独立状态快照覆盖内容、地址、容量、顺序、预算和网格 |
| 完整结果 | 评分逐路径分数；拓扑预算/邻接/队列与可执行修改序列；网格全部有效属性和规范化内容；不以布尔或数量相同替代等价 |
| 版本与兼容 | 新配对 v2、测量协议 1；场景/相机/目标 v1、发现 v2 保持；旧工程配对独立 v3 并声明诊断口径 |
| 配对关系 | 两动作 AB/BA，网格六排列，绝对块号贯穿预热；按目标、块和动作完整审计，禁止只挑部分成功记录 |
| 执行与发布 | 显式保存请求/实际线程、执行路径、回退与原因；真实 PID/RunId、前后源/构建/输入摘要；独占目录；失败保留 pending/rejected |
| 分析 | 固定配对中位差、MAD/标定/实际差阈值；来源和 W/R/P 分组；失败只作审计；无 bootstrap、正式拟合或研究继续决定 |
| 成本透明 | 目标复制/输入检查/验证/准备合计与中位数；累计重建不跨目标相加；C++ 内部、子进程与尝试间隔分别报告 |
| 注释 | 使用 `/// <summary>`；摘要正文按自然语义分句，标签不计入三行；新增关键边界注释逐项核查，覆盖率检查通过 |
| Git | 经用户许可，代码与文档按既有习惯分开提交；本地原始产物通过已有本机排除机制保存，未扩展版本库忽略策略，未推送 |

## 3. 初次实施的验证证据

| 核查 | 结果 |
| --- | --- |
| 实施前 | 两后端原有 32/32 CTest，保留源码与完整可执行基线；84 个性能进程记录、2,358 组指标噪声基线 |
| 实施后全量 | OpenGL 36/36、334.50 秒；D3D12 36/36、340.23 秒；均完整构建 |
| 最后专项 | 记录线程/状态约束、清单、实际进程配对和分析分组/成本补充后，两后端重建及相关专项通过 |
| 核心边界 | 五阶段、P=1/2/8、缺池、真实双线程、单/多安全块、W=0/1/5、R=1/2/6/30、来源/结果破坏 |
| 进程及分析测试 | 最终 8 项真实进程测试、12 项纯统计/分析测试；包括中断、缺失/不自洽来源拒绝、环境隔离、重复身份与第二轮不能洗掉首轮漂移失败 |
| 已完成实际子集 | Test129 中预算 18 目标，W=1/R=6；完整外层追溯和独立重建测试通过；不替代六场景验收 |
| 六场景完整集合 | 首次实际运行被 Peking 低预算 k=14 的细分拓扑不等价阻断；没有发布完成或研究结论 |
| 定向复现 | 两后端 P=8 实际应用失败；P=1 对照完成；独立诊断显示真实叶集合、队列、修改和最终网格均不同 |
| 最大规模容量 | 55,440 条合成策略行与 12,000 条标定行，同一包络另包含真实完成模板三次来源审计，2.819 秒、255.09 MiB；重复模板不构成独立研究运行 |
| 普通生产性能 | 156 个实施后记录与三个配置的 36 次交替复测完成，仍有重复恶化信号；不能通过性能验收 |
| 原行为和目标 | 普通矩阵十二组全部算法非计时字段一致；发现前后 24 次运行的冻结目标清单摘要一致 |

本节保留初次实施时的验证，后续修复与当前收尾见第 6 节。日志与运行路径以小规划第 11 节为索引，失败首轮及定向复现没有删除或覆盖。

## 4. 问题与处理

2026-09-10 当前处置：用户暂时关闭下文性能问题，允许提交后开始拓扑等价性修复；历史技术结论仍保留。后续工程性能判断采用开发规范第 7.3 节，几微秒差值不再作为持续阻断理由。拓扑 Critical 问题的标准和责任保持。

### Critical

**历史问题：原完整输出不等价，现已在已测范围修复。** 当前事实第 9 节与[定向分析](prep_04_split_topology_equivalence_analysis.md)保留修复前的失败：同一冻结输入上的并行辅助细分产生不同于串行的有效网格，当时违反小规划第 6.4、8.2 节。当前原反例与两后端全集已通过；不能继续把这条历史失败写成当前仍未修复，也不能据此宣称任意非空前缀都等价。

当时要求独立审查生产拓扑计划、提前提交与串行收敛在全局优先级和有限预算下的语义，再补真实反例及边界回归。后续已按用户确认的方案 A 实施；严格比较范围和 P=8 请求保持，没有删除目标或只比较三角形数量。

### Major（历史性能问题，已按用户决定关闭）

**性能验收不能通过。** 容量测试满足本机工程预算，但普通矩阵出现 25 个预定规则触发项，三配置复测后仍有恶化。规划前 144 次控制实验确认处理器分配与环境影响。用户批准[独立性能小规划](../../plans/formal_experiment/prep_04_runtime_performance_fix_plan.md)后，诊断关闭覆盖、普通版本测量工具与受控对照已实现，1,488 个有效应用进程及逐项审查见[独立性能调查第 8 节](prep_04_runtime_performance_regression_analysis.md#8-已批准性能小规划的实施与复核)。整帧版本方向不稳定，部分建堆差值仍不满足关闭契约，隔离链接结果不支持直接改生产设置。测量实现另有[结构审查](prep_04_runtime_performance_fix_architecture_review.md)；未直接优化、放宽阈值或自行延期。

当前事实没有显示职责不相关的大文件堆积、新反向依赖或重复生产策略实现。Runner 的职责仍是各实验模式的外层组织，算法测量与统计未堆回其中。

### Minor

已修正成本仅存在原始行、报告缺少逐目标汇总的问题，单次输入探测不重复相加；按执行身份、环境及 W/R/P 隔离跨运行方向，前后环境变化则停止胜负解释；强化来源完整性与 C++/Python 的线程、回退及 no-work 一致性约束。历史 PREP-02/03 事实文档增加范围说明和当前契约链接，避免把旧入口描述当作当前事实。

## 5. 收尾边界

2026-09-11 当前状态：下文按时间保留历史阻断与修复经过。已知第 14 帧不等价已修复，旧核心输入、新 OpenGL 全集和受控实际并行的已测范围通过；用户已明确关闭剩余性能问题。当前未观察到新的不等价反例，PREP-04 尚未完成的独立运行与自然非空前缀覆盖不足继续跟踪，详见[拓扑小规划第 9.7 节](../../plans/formal_experiment/prep_04_split_topology_equivalence_fix_plan.md#97-性能关闭后的拓扑状态)。性能关闭没有改变严格等价门禁，也不要求重复全部工程测试。

已按规划完成可审阅的实现、失败证据、性能调查和范围判断。完整目标集等价、全部名义重复、三次独立短运行稳定性和性能验收均未完成；当前不宣称 CPU crossover 成立或研究值得继续。生产策略及性能问题的修复方案另行规划和确认；现有实现经用户许可提交为 `7522db7`，文档单独归档，未推送。

2026-09-10 后续核对：文档提交为 `a738f78`。用户选择独立修复路径，已形成[拓扑小规划](../../plans/formal_experiment/prep_04_split_topology_equivalence_fix_plan.md)和[性能小规划](../../plans/formal_experiment/prep_04_runtime_performance_fix_plan.md)。拓扑提案明确同分顺序、串行停止边界、前缀分类及真实反例；放宽条件列为待验证提案。性能提案区分环境事实、剩余假设与生产修改 Review，避免未经定位直接优化。当前核查仅支持将具体方案提交人类 Review，不代表修复已经实施或验收。

其后用户以 Minor Revision 批准性能方案，测量实现与定向实验已按独立记录落地；当时生产修复原因仍未确定。用户最新决定已暂时关闭性能问题，并批准开始拓扑方案 A；此前“提案待 Review”和双重阻断均保留为历史记录，当前继续处理拓扑等价性。

2026-09-11：方案 A 已通过原第 14 帧、旧核心目标、新 OpenGL 目标全集及两后端工程回归，见[拓扑审查](prep_04_split_topology_equivalence_architecture_review.md)。新增规划成本由用户批准[独立空前缀修复](../../plans/formal_experiment/prep_04_split_prefix_planning_cost_fix_plan.md)；本轮按影响只运行三个专项、两个性能场景和一次相关场景复测，完整结果保持，中位数恢复，原非空前缀 P95 成本仍未关闭。已停止追加采样，未将工程测试精简解释为省略尚未完成的研究独立运行，PREP-04 未标记整体完成。

## 6. 2026-09-11 剩余运行收尾审查

本轮对照小规划第 7.1、8.2、11.9 节及开发规范第 5.8、7.3 节。用户关闭剩余性能问题后授权继续 PREP-04，执行前固定剩余运行范围；没有新增生产代码、公共接口、依赖或正式脚本。生产执行、只读输入与停表后取证仍由原模块负责，临时顺序编排和索引只保存在本机实验产物目录，未向 Runner 或算法类增加无关职责。

### Critical

已测范围没有新的结果不等价。当前版本 OpenGL、D3D12 各 113 个目标全集，以及 10 个固定首目标名义重复和两轮三次独立短运行，均通过严格配对和外层来源审计。72 个真实应用进程的共同目标输入/结果身份一致，两后端所选目标文件逐字节一致；原第 14 帧的修复证据保持，没有更换原反例或降低比较标准。

### Major

**采样环境稳定性未达标。** 首轮 22 个目标/动作中 21 个越过 5% 上限，冷却 95.554 秒后仅进行规划允许的一次复测，仍有 20 个不稳定。Peking 串行细分评分的绝对极差为 3.258900 ms，说明本轮限制也出现在毫秒量级动作；近零动作同时保留绝对差，未用极小分母制造影响结论。现有证据没有隔离漂移根因，也不构成新版本退化证据。已按规则停止采样，OpenGL 同一执行来源的全部 259 条目标比较排除胜负解释；D3D12 只验证全集，没有独立短运行稳定性证据。PREP-04 的运行与记录工作完成，但不能标记全部验收通过。

**自然并行覆盖限制保持。** 当前全集合并拓扑实际并行为 5/17 个目标，细分拓扑为 0/24 个目标，其余拓扑请求如实保留安全块不足回退。评分与脏网格各 24 个目标具有实际并行记录。六场景自然细分前缀为空，已有受控两块正例只验证执行能力，不能用于填补研究覆盖或证明普遍等价。后续 CPU gate 应保留这一限制；本轮没有启用放宽条件或作出投入决定。

原工程性能问题按用户决定关闭，既有数值和未决原因继续保留；本次环境检查不重新打开该调查。当前没有新增职责混乱、反向依赖或重复算法实现需要修正。

### Minor

验证选择与改动风险一致：只增量构建 D3D12 应用以纳入已验过的短路优化，复用 OpenGL 当前程序、此前两后端全量回归和优化后的三个相关专项，没有重跑 CTest 或普通性能矩阵。余下独立运行属于原协议的缺项，执行前已收缩为两后端各一次全集、10 个名义目标及必要的两轮短运行，未无限追加。

独立分析审计 4,980 条计量、874 条预热和 288,000 条标定，72 次空计时标定全部合格，耗时 7.969 秒、峰值 381.81 MiB。源码及两后端程序与本轮归档一致，输入和失败历史均保留。规划、事实和问题分析已同步当前结果，前文失败记录有明确时间范围；本轮无新注释或接口变更，沿用此前注释审查证据，未提交 Git。

证据索引见[小规划第 11.9 节](../../plans/formal_experiment/prep_04_minimal_cpu_pairing_plan.md#119-性能关闭后的剩余收尾)及[收尾报告](../../../benchmark-output/prep-04-topology-fix/resume-20260911/completion-report.md)。当前交付为实现、正确性和可追溯数据收尾，保留明确的环境验收限制。

## 7. 最后排查与移交决定

用户随后要求最后排查后写明限制进入 P5。源码已提交为 `ec8792e`；对 60 次现有短运行的独立重算没有发现数据混合、预热混入、重复进程或漂移计算错误，仍为 20/22 个动作组合不稳定。本次没有追加测量、改生产代码或重跑 CTest，详细证据见[最后核查审查](prep_04_timing_stability_final_review.md)。

按用户决定结束 P4 排查，保留第 6 节环境与自然覆盖限制，解除进入 P5 的流程阻断；不把工程移交写成环境验收通过。旧胜负排除和关闭的工程性能问题均保持，P5 需在新数据前显式确定探索契约。本次未改变职责、依赖和公共接口，未授权拓扑放宽或上传重构。
