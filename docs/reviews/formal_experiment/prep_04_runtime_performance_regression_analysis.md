# PREP-04：普通运行性能恶化调查

> 日期：2026-09-10\
> 状态：重复观察到恶化信号，尚未排除代码或构建因素；性能验收未通过，修复承接方式待用户确认\
> 依据：[PREP-04 小规划](../../plans/formal_experiment/prep_04_minimal_cpu_pairing_plan.md)第 9 节、[大规划](../../plans/formal_experiment/formal_experiment_preparation_major_plan.md)AC-12 与第 12.4 节

## 1. 判断

当前不能声明 PREP-04 没有性能问题。完整矩阵产生 25 个原始噪声规则触发项，其中 12 项在同期旧二进制配对中仍超过原噪声范围。对三个相关配置再做一轮独立交替复测后，OpenGL `standard / maximum-parallel-incremental` 的帧构建、证据收集等指标仍有恶化信号，串行配置的细分评分 heapify 中位数也仍需解释。

这些指标包含别名和相关包络，不能将数量当作独立统计证据。数据同时证明时段漂移显著，具体代码层面的根因尚未定位；因此结论是“性能尚未验证，需保留退化调查”，不是已证明所有变化都由代码造成。

本问题与[细分拓扑不等价](prep_04_split_topology_equivalence_analysis.md)分别处理。前者涉及普通运行成本，后者是既有策略输出语义，不得相互替代验收。

## 2. 原始方案与采集

实施前保留 HEAD `2dac3ea`、源码身份 `0f8b7994bf70ae1f98547b9c6a5353071c3466e95dbb893cc78ec32bbe117b70`、完整应用目录/DLL、CMakeCache 和编译器配置。两个后端分别执行 standard、budget-saturation × default、serial-incremental、maximum-parallel-incremental；每组一次预热和五次独立计量。另有六场景发现和策略回放。

原始规则在改代码前固定：新版本运行级统计的中位数恶化超过原五次基线极差即调查，不修改阈值。实施后普通矩阵交替使用保留旧二进制和新二进制；复测沿用相同命令、资产、参数和原噪声范围。性能进程未与构建或测试并行。

| 产物 | 内容 |
| --- | --- |
| `benchmark-output/prep-04/performance-before/baseline-20260909/` | 84 个实施前矩阵/发现进程记录，另有策略回放和保留二进制 |
| `performance-before/metrics-20260909/` | 2,358 组原始统计和预定噪声 |
| `performance-after/comparison-20260910/` | 156 个新/同期旧/发现进程记录，另有策略回放 |
| `performance-comparison/comparison-20260910/` | 4,710 组统计、原始差值、25 个触发项、同期配对及语义核对 |
| `performance-comparison/candidate-repeat-20260910/` | 三个疑问配置的 36 个交替复测进程，含各版本预热 |
| `performance-comparison/candidate-repeat-metrics-20260910/` | 复测运行级统计及与原基线的差值 |

完整矩阵绑定源码身份 `2af356d095f5e2d0ff0d6404353610adde34ec5a8ed17fc679a0763a4fbcacc4`。之后只补强 Python 离线来源/环境审计与成本汇总及其测试，普通应用、发现流程和二进制未改变；三配置复测绑定最终源码身份 `d851f4b3b8ee447b0e5467e3feeebf7f77202b6de12530e174561359cc7c1b71`。两个源码身份分别保留，不能写成同一个快照。

## 3. 主要数值

以下“配对差”先在相邻旧/新进程之间计算同一指标之差，再取五个差的中位数；它不等于两个中位数直接相减。单位均为 ms，只有进程总时间一行为秒。

| 指标 | 原基线中位数 | 原极差 | 首轮同期旧中位数 | 首轮新中位数 | 首轮配对差 | 复测配对差 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| OpenGL 最大并行：帧构建中位数 | 9.02075 | 0.28850 | 9.25910 | 9.49275 | +0.32960 | +0.59930 |
| 同配置：帧构建 P95 | 10.84594 | 2.22502 | 11.84099 | 13.99359 | +2.83975 | +2.92133 |
| 同配置：证据收集中位数 | 5.89330 | 0.07115 | 6.08165 | 6.50975 | +0.41745 | +0.16310 |
| 同配置：证据收集 P95 | 6.67819 | 0.56555 | 7.26993 | 7.91221 | +1.24171 | +1.26339 |
| 同配置：进程时间（秒） | 0.69696 | 0.04122 | 0.69875 | 0.75869 | +0.03099 | +0.04628 |
| OpenGL 串行：细分评分 heapify 中位数 | 0.06270 | 0.00120 | 0.06305 | 0.06505 | +0.00205 | +0.00440 |
| D3D12 饱和/default：合并评分 heapify P95 | 0.53910 | 0.12694 | 0.47755 | 0.71549 | +0.28099 | −0.05415 |

D3D12 该项恶化未在复测中重现。OpenGL 最大并行的若干评分别名、收尾与候选快照指标也触发，详见 `followup-triage.json`；不能只选较好子项宣布通过。

时段漂移的直接证据：相同保留旧二进制的帧构建中位数，从首轮同期 9.25910 ms 上升为复测同期 10.69720 ms；新版本相应为 9.49275 ms 和 12.79095 ms。复测五个帧中位数差为 `[2.23085, 0.59930, 4.97530, 0.26825, -0.26840]` ms，并非每次都同方向。这个分布不足以把根因归结为单个新增函数，也不足以排除版本相关问题。

## 4. 已完成的排查

1. **核对实际工作量与结果。** 普通矩阵十二组的原始 CSV，排除时间和 CPU 利用率后，全部非计时字段逐列一致。最初的粗摘要把 `cpuUtilizationPercent` 也纳入“语义”，它会随运行时间变化；差异已定位并单独处理，没有改写原始 CSV。发现前后共 24 次运行的目标清单 SHA-256 只有一个值，目标身份和选择未变化。
2. **核对同期旧程序。** 首轮已逐进程交替运行旧、新二进制。13 个初始触发项未在同期配对中超过原噪声范围，其余项进入三配置复测。原始全矩阵和首轮异常全部保留，未用后一次覆盖前一次。
3. **核对测量路径。** 普通 benchmark 的证据收集位于 `DataOrientedRoamPipeline.cpp::CollectPassEvidence`，本阶段未修改该函数或生产评分算法。新增 `PassMeasurement`、`PassEvidence` 的完整验证只由配对/旧冻结诊断使用，不能据函数名相似就将普通 `passEvidenceMilliseconds` 的变化归因于新验证器。普通矩阵没有启用新的 CPU pilot 配对入口。
4. **区分新能力成本。** 配对复制、检查和重建单列；新旧工程配对的 wallMs 口径不同，未作速度比较。分析最终容量复测为 2.819 秒、255.09 MiB，包含显式合成大表和真实完成模板的来源审计成本，不是普通应用帧时间。
5. **区分后续 Python 审计修正。** 最后来源完整性、环境分组和单次探测成本修正不在普通 benchmark/发现执行路径内，且两轮使用相同应用二进制。它们不能解释本表中的 C++ 帧时间差。

## 5. 原因与尚未确认的部分

已证实：原始粗语义摘要差异来自利用率观测；原旧程序本身存在显著时段漂移；部分恶化只出现一次；OpenGL 部分指标在两轮配对中继续触发原规则。算法有效工作量增加目前没有证据支持。

UNCERTAIN：普通运行中版本相关差值尚未定位到具体指令、分配或调度事件。编译/链接布局、缓存行为与系统调度的影响尚未拆开验证；这些只是后续排查方向，不能作为已证实原因写入结论。当前也不能仅因相关生产函数源码未改就认定没有性能回归。

## 6. 需要用户决定的承接方式

建议新增独立性能修复小规划：先保留本次原始基线与两轮证据，明确受影响包络，进行可复现的调用/编译产物和运行环境定位，再提出有证据支持的修复。具体生产文件归属应由定位结果决定，不能先猜一个优化点就改代码。

另一方式是经用户明确允许后修改后续规划承接，并写明阶段、验收责任及可继续范围。无论采用哪种方式，复测仍需对照 PREP-04 退化前原始基线，不能以当前较慢结果替换唯一基线。细分拓扑不等价仍会独立阻断完整 CPU 配对验收。

依据用户要求及小规划第 9 节，当前只完成测量、复现与分析记录，未实施性能优化、修改阈值或自行批准延期。
