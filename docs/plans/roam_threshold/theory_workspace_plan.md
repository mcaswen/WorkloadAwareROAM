# ROAM 阈值闭包理论工作区小规划

> 日期：2026-09-12
> 授权：用户要求先提交现有修改，再直接建立推导工作区并自主推导与形式化验证
> 类型：独立理论研究材料，不是运行时算法接入规划
> 前置提交：`1601717`，当前 CPU ROAM 研究定位及历史入口整理

## 1. 目标与边界

先检验固定细化依赖、阈值最小闭包和硬预算判定能否形成完整证明链，再分析完整并行成本。产物区分机器检查的抽象定理、纸面几何论证、已有文献结果、反例和尚未证明的实现对应关系。不得以有限例子或自定义公理代替待证结论。

本轮只创建理论文档与独立 Lean 证明，不修改 Classic/DOD、CMake、评价器、基准输入或已关闭探索。当前实验问题定义保持有效。新工作区最初按授权保留供审阅；用户随后认可本轮理论结果，并明确授权将这些材料独立提交。

## 2. 文件归属与依赖

创建 `docs/research/roam_threshold/`：

| 文件 | 职责与独立理由 |
| --- | --- |
| `README.md` | 范围、状态、结论索引、复现入口 |
| `model_and_proofs.md` | 事件、闭包、阈值、预算的定义与纸面证明，以及网格对应义务 |
| `algorithm_and_limits.md` | 阈值谱算法、总工作量与关键路径、质量和增量限制、反例 |
| `sources.md` | 已核对文献、已有结论、源码证据和候选差异 |
| `verification.md` | Lean 版本、运行命令、定理清单、可信边界及审计结果 |
| `formal/Closure.lean` | 固定关系上的闭包与相对闭包定理 |
| `formal/Threshold.lean` | 层次叶误差与必选集合的等价、最小闭包与预算可行性 |
| `formal/Spectrum.lean` | 将阈值闭包写成依赖传播后的标量激活条件 |
| `formal/Counterexamples.lean` | 对过强命题给出可由内核检查的有限反例 |
| `formal/lean-toolchain` | 固定独立证明工具链版本 |
| `formal/.gitignore` | 忽略编译证明与本地缓存 |

依赖方向为 `Closure → Threshold → Spectrum`，反例独立；论文和代码事实仅作为建模依据。复用项目内 ROAM 原文转录、源码对照与已冻结参考定义，不复制业务实现。Lean 工具只存放在已忽略的 `tools/downloads/lean/`，不进入第三方源码或生产依赖。

## 3. 推导顺序与停止条件

1. 对标准二分层次建立逻辑事件与固定前置关系的纸面对应；独立证明一般关系闭包性质，明确二者的验证层级。
2. 从叶条件证明必选集合的必要性与充分性，不把这条等价直接作为公理；显式处理初始网格和不可细分终端。
3. 推导最小阈值闭包、成本与硬预算判定，检查能否得到阈值谱表示。
4. 分析完整算法成本，给出反例与未完成义务，审计所有定理假设。

若网格对应、误差单调性或几何上界无法证明，保留具体缺口，不通过更换评价目标或假设结论绕过。抽象证明仍可作为有明确适用范围的结果，但不得宣布实际 ROAM 算法已经验证。

## 4. 验证与性能口径

使用 Lean 内核检查无限量化的抽象命题，禁止 `sorry`、`admit`、自定义 `axiom` 和 `native_decide`。打印核心定理公理依赖；允许标准逻辑基础，但逐项披露。小反例使用内核可归约的有限判断，标为反例而非普遍证明。

本轮没有生产代码或构建变化，不运行无关 CTest、图形后端矩阵或 ROAM 性能测试。只记录证明检查自身耗时，不能将其解释为未来 CPU 算法成本或性能前后对照。链接、UTF-8、未跟踪产物、证明假设和文献新颖性边界做定向检查。最终审查写入 `docs/reviews/roam_threshold/theory_workspace_review.md`。

## 5. 完成情况

2026-09-12 已完成本轮理论工作区，入口为 [README](../../research/roam_threshold/README.md)。

- 原有研究定义修改已先提交为 `1601717`；新增工作区、小规划与审查文档经用户认可后获准独立提交。
- 4 个 Lean 文件共 36 项定理、引理和反例通过内核检查，所有声明均完成公理依赖审计；没有跳过证明的占位项或自定义公理。
- 证明包括固定闭包、相对闭包、成本次模性、阈值必选集合、最小闭包、禁止终端、硬预算等价及激活值表示。证明摘要、指纹与复现见[验证记录](../../research/roam_threshold/verification.md)。
- 对标准规则二分网格给出纸面事件对应；确认 triangle-event 图包含配对环，收缩 diamond 后才适合 DAG 执行。
- 推导全层次阈值选择算法及包含优先级计算和输出的 work/span；持久状态更新另列未解成本，不报告真实 speedup。
- 给出 bilinear reference 的有限世界高度极值算法与正投影深度条件下的屏幕上界，标为纸面推导。没有把它接入评价器或当前 score。
- 文献核对表明阈值细化、依赖图和误差饱和有直接先例。本轮不宣称算法新颖性或研究路线已经通过性能 Gate。

与规划比对：未修改 Classic/DOD、CMake、现有研究问题或冻结实验输入；未重新开启已归档路线。仅运行本轮证明检查与文档审计，不运行无关性能及后端测试。Lean 二进制、编译产物和检查日志均被忽略。审查见[理论工作区审查](../../reviews/roam_threshold/theory_workspace_review.md)。

## 6. 后续授权：增量维护理论 Gate

用户认可并提交首轮工作区后，要求单独判断连续视点下的 activation spectrum 与 hard-budget target 能否按受影响区域维护。该 Gate 只做纸面算法、成本证明和定向文献核对，不增加 C++、Lean、并行平台或实验矩阵。

文件边界：复用现有模型和证明；新增 `docs/research/roam_threshold/incremental_gate.md`，集中记录跨帧状态、增大/减小算法、阈值维护、视点更新的完整成本及 prior-art；更新工作区 README 的状态入口。完成自审写入 `docs/reviews/roam_threshold/incremental_gate_review.md`。不再创建单独的工具或来源管理模块。

出口按用户约定：只有明确的新机制、非平凡局部复杂度和与既有工作的实质区别同时成立才 Pass；否则根据所证边界及已有方法覆盖情况作 No-Go，关闭本候选支线。不得把显式全谱的下界推广到全部隐式表示，也不得将“未找到完全相同论文”当作新颖性证明。

核查只覆盖公式、计数、证据出处、本地链接和修改范围；此前 Lean 结果直接复用。论文下载和文字提取放在已忽略的 `build/roam-threshold-incremental-gate/`，不进入仓库。完成情况以 Gate 报告及审查为准；用户认可本轮结论后已授权独立提交。

完成结果：已给出 source 增减的常数扇入维护、完整加权阈值与集合差分算法、`Tsource + O((s+k+1) log n + |ΔJ|)` 条件界，以及统一 priority 扰动下 `|Δτ|≤δ` 的纸面证明。对显式 spectrum、逐点 oracle 和惰性证书分别分析下界与适用范围，定向核对 7 项原始工作。最终 **No-Go**，停止该候选支线，返回原 execution/characterization 主线；详见 [Gate 报告](../../research/roam_threshold/incremental_gate.md)及[完成审查](../../reviews/roam_threshold/incremental_gate_review.md)。
