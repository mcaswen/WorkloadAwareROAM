# NMP-01：严格动态目标决策与生产状态修改解耦

> 日期：2026-09-13；类型：小规划；上层：[原生接入大规划](native_materialization_plan.md)。
> 状态：**NMP-01 实施与验收闭环完成**。固定输入范围内严格语义解耦通过；节点/义务规模局部，压力点堆覆盖较广；当前 Planner 成本明显高于 Legacy，三项分别判定，详见 §11.12。未实施 NMP-02，代码未提交。
> 源码基线：`602ed48`；保留 MPR 的独立直接物化证据。

## 1. 本阶段问题与出口

验证同一个已评分细分阶段输入 M₀，能否在不写生产拓扑、不复制全状态、不调用旧提交器的情况下，得到与 Legacy 严格执行相同的目标及续接义务：

\[
M_0\xrightarrow{\text{独立稀疏逻辑规划}}(J,\Delta,\Gamma).
\]

本阶段只实现第一箭头。验收分成三个独立结果，不能用一个 Pass 掩盖另一个缺口：

1. **语义解耦**：J、Δ、封闭 Γ、完整严格决策轨迹和逻辑堆投影一致；来源未修改，出口不携带目标生产状态。
2. **局部性证据**：报告实际节点/堆覆盖、读取和义务规模；判断当前自然输入是否仍具有稀疏结构。
3. **成本记录**：测 T_plan 及共享规则提取对原实现的影响。第一版覆盖容器较慢不自动否定前两项。

通过定向测试是该输入范围的实现证据，不等于对所有 ROAM 状态完成形式化证明。NMP-02 才验证出口足以独立恢复完整原生状态；NMP-01 不提前声称生产续接和完整更新加速已经成立。

## 2. 范围、已读事实与实现边界

已核对[开发规范](../../standards/development_guidelines.md)、[规划规范](../plan_guideline.md)、[接入事实](../../codebase/roam_parallelism/native_materialization_integration_baseline.md)、大规划及用户七项评审。定向重读 `DataOrientedRoamTopology.cpp` 的严格步进/预算策略/强制递归/观察入口，`TopologyExperiment.h`、`Queues.h/.cpp`、`Scoring.*`、原型探针的自然/压力来源和现有测试构建。

关键事实：

- 原控制器不仅细分，还先执行低分合并及失败后的预算交换；根失败可保留成功前置。
- 循环头部的 `<`、迭代后增、强制前置的 `forcedFrom` 都有行为意义。当前递归的 guard 达界只结束该 while 条件，随后仍按源码进入后续判断；不能自行新增“guard 达界立即失败”的语义。
- `Q_s/Q_m` 都是持久堆，新成员会在本阶段评分；不能用入口候选列表代替动态队列。
- 现有 `AdvanceStrictSplitIteration` 可观察循环体结果，但不足以完整观察根内部的预算和前置。
- `ExecuteObservedSplitRoot` 每根计算完整输入哈希，`AnalyzeSplitOperation` 还克隆状态；二者均不适合作为本阶段规划器或轻量轨迹的实现基础。

本阶段新增逻辑规划核心与无窗口测试/探针，必要时提取纯规则。**不实现原生物化、网格载荷、GUI、生产新路径派发或并行执行**；不修改 MPR 状态/补丁/参考。

原／新开关仍按大规划保留。NMP-01 固定枚举与 CLI 名称的设计，公开字段/实际派发随 NMP-02 接线，避免当前出现一个可选但尚未实现的生产动作。共形来源标记及新生产载荷也在 NMP-02 接入；本阶段输入由外部诊断认证。

## 3. 文件归属与职责

源码根 `DOD = src/algorithms/data_oriented_roam/`，新增核心目录 `DOD/materialization/`。按下表真实需要创建，不生成未来物化器空壳。

| 决策 / 文件 | 本阶段职责与理由 |
| --- | --- |
| Create `materialization/NativeMaterializationTypes.h` | 固定 Δ、Γ 类型、最终预算/停止摘要、求值缓存和规模结果；不定义完整目标 Node/State |
| Create `materialization/NativePlanningView.h/.cpp` | 借用 M₀、稀疏关系/标记覆盖、新逻辑身份及预算；存储不负责选择根 |
| Create `materialization/NativePlanningQueues.h/.cpp` | 私有两堆覆盖、逻辑长度、反向位置和代表关系；无生产堆导出接口 |
| Create `materialization/NativeRefinementSimulation.h/.cpp` | 根内部必要的逻辑细分/合并、强制前置和局部候选演化；不写原生索引/mesh |
| Create `materialization/NativeTargetPlanner.h/.cpp` | 独立严格根选择、预算交换、阻塞/停止和净出口提取；不变成第二个含生产发布的 Topology 大文件 |
| Create `DOD/DataOrientedRoamIndexedHeap.h` | 共享静态内联 sift/restore 等堆原语；直接与覆盖访问器分别实例化 |
| Create `DOD/DataOrientedRoamDecisionTrace.h` | 轻量决策事件的值类型和无操作诊断策略；不存储全局 trace、状态引用或 CSV |
| Extend `DOD/DataOrientedRoamQueues.h/.cpp` | 提取分数/PathId 比较和固定局部描述上的资格规则，原访问仍直接读数组 |
| Extend `DOD/DataOrientedRoamScoring.h/.cpp` | 域/误差描述评分入口；原节点外壳调用同一公式，不让 Planner 调用真实 AddNode 才能评分 |
| Extend `DOD/DataOrientedRoamTopology.cpp`、`TopologyExperiment.h` | 原控制器的编译期轻量决策观察入口；保留独立 Legacy 控制流与原生产 mutator |
| Create `tests/DataOrientedRoamNativeTargetTests.cpp` | 纯规则/覆盖/严格轨迹/Γ 与边界夹具；保留首次差异上下文 |
| Create `tests/DataOrientedRoamNativeMaterializationTestSupport.h` | 外部来源恢复、独立参考投影和测试专用审计读取；不被生产源码包含 |
| Create `tests/DataOrientedRoamNativeMaterializationProbe.cpp` | 本阶段只提供目标诊断、Legacy 与 Planner 短计量；未来复用同一原生接入探针 |
| Extend `CMakeLists.txt`、`tests/CMakeLists.txt` | 新核心进入 DOD 编译清单；测试/探针单独链接来源、记录与验证能力 |

共享纯层次提取本阶段暂不实施：规划新身份可沿当前父记录用已有 `LeftChildPathId/RightChildPathId` 和 `SplitTriangleDomain` 构造。NMP-02 需要一般伙伴/精确边键时再提取 `RoamHierarchy`。因此 NMP-01 不必为目录对称改动 MPR。

依赖方向：测试/探针 → 新规划器与 Legacy；新控制器 → 逻辑模拟 → 视图/堆 → 原生只读类型及共享纯规则。Legacy 不依赖 `NativePlanningView`，新规划器不依赖旧控制器、实验 Bridge、GUI 或渲染。生产和测试只共享值格式及纯计算，不共享新旧高层控制器。

## 4. 规划状态与正常出口

### 4.1 三层数据必须分开

| 数据 | 使用者与寿命 | 是否可以交给物化器 |
| --- | --- | --- |
| M₀ | 只读借用，由调用方持有；整个同步规划有效 | 作为旧状态使用 |
| 私有规划视图、虚拟下标和堆覆盖 | 仅在规划调用内参与动态决策；测试可在销毁前审计 | **不可以**；不保留到正常返回结果 |
| `NativeTargetPlan` | Δ、封闭 Γ、逻辑摘要和纯求值缓存 | 可以；没有目标邻接/活动数组/生产堆布局 |

拟定普通接口为 `NativeTargetPlan BuildNativeSplitTarget(const DataOrientedRoamState& source)`。审计入口另设诊断策略，在返回前向测试只读访问器提供逻辑投影；诊断访问器不能被 `NativeTargetPlan` 持有，也不作为未来物化的输入。

私有目标和轨迹均从独立 schema 1 开始，记录版本与基态输入身份；不序列化为可跨帧发布的生产事务。现有 `DataOrientedRoamPassInputVersion` 保持不变，后续新增生产载荷时另作版本扩展。公开选择名沿用大规划的 `TerrainLodSplitTopologyImplementation::{Legacy, DirectMaterialization}` 和 `--dod-split-implementation`，本阶段不暴露未接线选项。

私有视图按字段借用原池：已有节点域、误差、身份、父子关系首次读取不必创建完整覆盖记录；仅变动项建立覆盖。新虚拟节点保存为决定后续分数所需的域、误差索引/值、身份及必要关系。节点活动资格与 `IsSplit` 分开表示，不能把休眠缓存误当活动叶。

逻辑关系允许保存当前三邻接，因为下一步 forced 前置可能读取它们；但它们属于决策工作区。正常出口提取完成后销毁，最终叶邻接必须在 NMP-02 重新由局部目标构造。

不维护生产活动数组的顺序/反向活动位置、NodeSlots、SlotOwners、CPU 网格和 TopologyEdits。只更新活动数量及受影响节点的逻辑资格；不每帧遍历全池建身份字典。只读量与覆盖量分开记录，防止“没有写全域”掩盖“读取了全域”。

### 4.2 Δ 和 J

Δ 以**三角形路径事件**为单位：排序去重的 `AddedEvents/RemovedEvents`。`k` 是二者之和；不按请求根或菱形组计数。

从实际触及事件的入口/最终逻辑活动状态提取净变化；反复修改回到初值的事件不进入 Δ。J 是基态事件谓词加这份差分，不构造完整 J 集合。外部验证器可以全量展开 J 比较，费用只属于诊断。

### 4.3 Γ 字段白名单

禁止 `std::any`、任意字段快照、目标节点数组、最终叶边/堆/mesh 载荷。使用以下四类有类型的局部记录；实现时逐字段列出写入点、原生后续读取者和无法由 Δ 唯一确定的理由。

| 类别 | 最小语义载荷 | 独立对照来源 |
| --- | --- | --- |
| `CandidateDisposition` | PathId、细分本帧阻塞或失败合并候选最终移除、原控制分支原因 | Legacy 的实际阻塞/移除事件及最终队列；后续重新插入时撤销已经失效的处置 |
| `EpochAndSourceChange` | PathId、发生变化的轮次字段/强制来源/本轮路径记录 | Legacy 前后字段与成功事件；同字段只保留最终必要值 |
| `CacheBirth` | 新缓存 PathId、必要创建轮次 | Legacy 完整前后缓存身份差；不含物理下标、创建槽位、域或整份节点 |
| `InternalRelationChange` | 非目标活动叶节点 PathId、已批准的 Base/Left/Right 关系字段、目标 PathId 或空 | Legacy 前后内部关系与读取者；需要证明其不能从 M₀+Δ 局部推导，不能整行复制三邻接 |

没有续接后果的根失败和每次预算变化只进入决策轨迹/摘要，不进入 Γ。由身份、Δ 或当前环境直接恢复的父子、域、深度、默认轮次不重复编码。新缓存身份可由最终活动结构推出的部分也不重复登记，仅缓存存在性超出该结构的必要义务进入 Γ；`g` 仍独立记录全部逻辑新建节点。

`InternalRelationChange` 是风险最高的一类：**字段名白名单不等于已证明必要**。步骤 1 先完成逐字段理由和小反例；不能说明必要性的字段暂列审计差异，不进入 Γ。若某个旧休眠字段可被未来激活安全覆盖，就不能以“原数组不同”为理由强行保存。

规范化 Γ 的键为“类别、PathId、字段”，同键折叠，最终值等于可恢复值则去掉。`h` 是规范化义务数量；另报每类条目、不同身份、载荷字节和提取前峰值，不按一整个 vector 计一个义务。比较 Γ 时使用固定语义规范化规则，新旧两端各自产生，不把 Planner 输出交给 Legacy 投影器当查询白名单。

### 4.4 纯求值缓存与摘要

可复用 Planner 确为选择计算过的域、几何误差和纯 score，按稳定身份与冻结环境键索引；不携带活动/候选资格、邻接、堆位置或槽位。逐位保持当前共享评分计算顺序，不能利用缓存为未来批量阶段提前做无关计算。

最终预算、停止原因、迭代位置、根/强制/合并成功失败计数属于固定逻辑摘要，不携带事件列表。求值缓存可丢弃后重新计算，Γ 不可被缓存替代。NMP-02 必须验证关闭求值缓存仍可构造相同结果；本阶段仅固定并检查载荷边界。

## 5. 独立严格控制器及堆覆盖

`NativeTargetPlanner` 只管理严格循环、选择与停止；`NativeRefinementSimulation` 管理一个根内部的逻辑前置展开/合并；`NativePlanningView` 只负责存储与局部查询。不要将原 `Topology.cpp` 整段复制后把写者改名为 Planner。

共享边界限于：分数与 PathId 比较、给定局部描述的资格、迟滞谓词、纯评分和索引堆基本算法。原串行入口仍直接访问原数组；不得把 Legacy 套进覆盖访问器。Planner 不调用 `RunStrictSplitStep`、`AdvanceStrictSplitIteration`、`SplitNodeImpl`、生产 AddNode 或原队列 mutator。

每轮保留以下逻辑顺序：先低分合并 → 细分头部停止判定 → 根预留与动态强制前置 → 根据部分结果再次读取队首 → 预算交换或阻塞/停止。新评分、新候选和失败后的状态立即参与下一次逻辑判断，不延迟到整个规划结束。

覆盖堆用 `std::map` 保存变化槽位，另保存逻辑长度、反向位置及代表/伙伴覆盖。读取未覆盖且未被截断的槽位才访问基态；删除再追加不得复活旧尾部。分数、资格变化后即时修堆。正常返回前仅用它完成决策/审计，不输出物理堆补丁。

正确性不依赖覆盖稀疏程度；稀疏程度只影响空间、查找成本和方向价值。生产队列的最终恢复属于 NMP-02，不能靠导出整个覆盖堆提前完成。

## 6. 轻量严格决策轨迹

### 6.1 事件契约

固定事件以稳定路径和明确结果为内容，测试侧定义比较，不纳入生产 CSV：

| 事件 | 需要比较的内容 |
| --- | --- |
| 根选择 | 迭代序号、Split/Merge、根 PathId、选择时分数、普通低分合并或预算交换来源 |
| 前置尝试 | 当前根、当前尝试 PathId、forcedFrom、Requested/Forced、进入及结果 |
| 预算操作 | 预留成功/拒绝/释放、所属尝试、前后余量；合并释放按原 primitive 数量计 |
| 根完成 | 成功/失败、是否发生预算拒绝；部分成功前置单独保留 |
| 处置与交换 | 被阻塞/移除候选、代码分支原因、交换尝试与成败 |
| 停止 | `NoEligibleSplit/BudgetBlocked/IterationLimit`、最终余量、迭代位置和入口上限 |

按严格根决策顺序和根内逻辑前置/预算因果关系比较；不记录堆交换、节点数组赋值、分配槽位和线程时间线，不把此轨迹当必要 CPU 关键路径。首版逻辑模拟遵循原递归前置顺序，不另外研究可交换重排。

失败原因只按实际分支分类，例如深度拒绝、预算拒绝、前置失败或根非预算失败；原代码没有明确区分的原因记录为该原分支，不能为了丰富报告改变控制逻辑。

### 6.2 获取方式与隔离

Extend 原控制器/commit policy 的编译期诊断策略，关闭时是无操作对象，无每事件虚调用、分配或字段观察。轻量 Legacy 入口运行原严格代码，不调用每根完整哈希观察器，也不克隆每一个根。

Planner 由自己的控制器和逻辑模拟独立发事件；两方共享事件格式而非事件发生判定。测试外层从同一 M₀ 产生 Legacy 工作副本，原状态供 Planner 只读借用，比较前后来源与最终参考。外部克隆、完整投影和轨迹存储都不进入普通 Planner 调用图。

解析夹具完整逐字段比对；自然点同样不得只比较根前缀。每条轨迹首版限制一百万个固定事件，不记录完整字段足迹；达到限制则记为“轨迹审计未完成”，保留已知结果并停止扩展，不把截断摘要标为等价。首次不匹配只输出邻近事件与字段，完整原始记录留忽略目录。

诊断开启/关闭的 Legacy 和 Planner 各自结果必须一致。计时仅使用关闭轨迹和全量审计的入口。

## 7. 外部正确性与局部性审计

### 7.1 比较对象

外部 Legacy 参考从同一 M₀ 调用原 `SerialImmediate` 阶段。其状态投影完整枚举缓存、活动事件及队列，独立得出 J、Δ、Γ 和规范化逻辑堆；不得只检查 Planner 宣称变动的节点。

Planner 的完整逻辑堆只在诊断回调中按 PathId/分数/代表/伙伴和抑制状态读取；检查自身 heap/reverse-position 不变量，物理槽位顺序不作为新旧等价条件。分别检查两个方向的成员缺失/多余，避免只比较队首。

源状态只读检查包括节点、活动数组、队列、历史、预算、mesh 元数据、容器长度/容量及数据地址；构造诊断快照属于测试开销。普通 Planner 不得通过 `const_cast` 修改源统计或扩容源容器。

归纳检查按“入口逻辑投影一致 → 当前根/分支一致 → 根结束后的队列/预算/标记一致”组织夹具。它用于定位差异，不引入通用 Lean/符号执行平台。本阶段尚无原生物化，所以不以继续运行一个伪造 M₁ 的方式替代正确性比较。

### 7.2 计数定义

| 字段 | 口径 |
| --- | --- |
| `N`、`Qs/Qm` | 入口缓存数量及两个堆长度；另报入口活动叶数 |
| `a`、`aPeak/aFinal` | 规划期间累计不同被覆盖节点、同时保留峰值及最终存量；旧记录和新虚拟记录分列 |
| `r` | 逻辑节点字段/记录查询的操作次数，重复访问计数；不称为 C++ load 数 |
| `u_s/u_m`、`uPeak/uFinal` | 每堆累计不同被改写的槽位及峰值/最终存量；原槽位和新追加区间分列 |
| `uMembership` | 反向位置、代表/伙伴覆盖单列，不拿它和堆槽位混在同一 u/Q 比值 |
| `L` | 堆槽位访问/修复操作次数，重复访问计数；比较次数另计 |
| `h`、`g`、`k` | 规范化 Γ 义务、全部逻辑缓存创建、净事件差分数量，保留单位差别 |
| 读覆盖 | 诊断额外记录不同旧节点/堆槽位读取数量，识别大范围读取但少量写入 |
| 内存/其他工作 | 覆盖峰值、出口/求值缓存载荷、评分次数、净差分排序量、销毁成本；不凭容器行数推总内存 |

诊断给出 `a_old/N`、`u_s_old/Qs`、`u_m_old/Qm`、不同旧读数量比例、h/N 和 h/k。零分母记不适用，新增虚拟记录另列，比例不能靠删掉中间覆盖项缩小。自然输入中的小比例只是覆盖证据；三个点不能证明普遍 O(k) 或全域规模无关。

## 8. 运行输入与最小验证

### 8.1 解析夹具

一个定向测试目标覆盖：

- 平分 PathId、迟滞阈值边界、停止头部不能越过、先合并 `<` 边界。
- 根预留、强制前置、部分前置成功后失败、释放范围、失败后的新合并头部和预算交换。
- 最大深度、已合并/已细分抑制、非预算阻塞、缓存复用、净差分为空但历史处置非空。
- 堆任意删除、尾部换位、重复追加/截断、伙伴代表变化、空堆与抑制值。
- 迭代上限公式和后增停止边界；人工计数器边界标为控制组件夹具，不冒充自然轨迹。
- 正常出口字段白名单、Γ 规范化、源未变、独立控制器调用边界；诊断不能改变结果。

能够以少量针对性夹具覆盖就不重复全套 MPR 的 2304 对状态矩阵。本阶段不改 MPR 构造，既有 MPR 结果直接引用；共享评分/堆若影响现有路径，选对应旧边界测试。

### 8.2 冻结自然输入

| 输入 | 固定来源与参数 | 使用方式 |
| --- | --- | --- |
| `test129-a-b4096` | 既有轨迹 A、sample14；30/4、H=14、B=4096、阈值 4/2 px | 复用 MPR 小差分现实输入，不改成另一个预算 |
| `peking547-a-b20000` | 既有轨迹 A、sample14；80/12、H=20、B=20000、阈值 0.25/0.1 px | 非 dyadic 自然输入 |
| `peking547-budget-orbit64-b200000-sample14` | MPR-03 既有压力协议，80/12、H=20、B=200000、0.25/0.1 px | 大状态/大差分 reality check，不与轨迹 A 混称 |

三者均按原序列从干净状态推进 0..14，在已评分细分入口借用 M₀；前面运行 Legacy 只是恢复共同旧输入，目标 J 必须由新 Planner 自己决定。不能调用 `MaterializationDodBridge::Capture` 取得新路径答案。

来源夹具持有高度图、流水线和测试用状态副本，直到该案例的诊断/计量结束；借用的 HeightMap、误差环境和线程池地址不得指向已销毁的局部对象。状态副本只属于外部参考/计量恢复，不把其所有权传入正常 Planner，也不保存在输出计划中。

视图继续使用冻结 CPU 投影、1280×720、60°、0.1/500；压力先四点来源热身并销毁该状态，再恢复固定前缀。资产尺寸/hash 与清单核对，来源记录和完整输入 hash 在计时外生成。WSL/Linux 与旧 Windows 数据不作速度配对；必要浮点位差单独登记，不更换输入寻找同值。

诊断每点各完成一次完整新旧轨迹、目标及逻辑堆比较，不在每次计时后再跑全域验证。失败时先保留反例并处理/Review，不继续剩余性能矩阵。

## 9. 快速性能契约

### 9.1 基线与计量

先完成只包含来源恢复与 Legacy 计时的中性探针并冻结其分支，然后再修改生产共享规则/诊断入口；保留基线源和二进制身份。实现后使用相同 Legacy 分支测共享抽取影响，另测普通 `BuildNativeSplitTarget`。不以 MPR 的逐菱形参考时间代替原生基线。

默认 **三个计量进程**：修改前 Legacy、修改后 Legacy、修改后 Planner；每进程包含上述三个固定点，每点一次热身、两次短计量。另一次后版本诊断进程完成正确性与覆盖计数。没有线程矩阵、5×30 或完整 64 点性能采样。独立进程为统计单位，本轮只做工程筛查，不用两个重复推断稳定 P95 或统计显著性。

每次计时前由测试外层恢复同一旧状态；为了匹配入口内存条件，Planner 也在计时外得到同协议的工作输入。恢复、来源、输入认证和输出文件费用单列。计时中不克隆、不调用旧阶段、不做全量认证。

`T_plan` 包含构造覆盖、逻辑模拟/堆维护、新评分、Δ/Γ 提取及求值缓存整理，直到私有工作区销毁和函数返回。结果所有权返回后的析构另记 `ResultCleanupMs`，以后合入完整新阶段，不将它漏掉。诊断回调的全量投影时间不用于速度比较。

报 `LegacyBeforeMs`、`LegacyAfterMs`、`PlanMs` 及对应身份。`PlanMs/LegacyAfterMs` 仅表示“规划费用相对旧完整细分阶段的比例”，两者输出任务不同，不能称为加速比。`MaterializeMs` 和新完整阶段时间留空。

### 9.2 冻结关闭项

正式计时调用前将以下设置/入口固定为：

| 项目 | 计时配置 |
| --- | --- |
| `EnableTopologyValidation` | false |
| `EnablePassEvidence` | false |
| `EnableTopologyPairEvidence` | false |
| `MirrorSplitScoresToNodePool` | false；来源和对照保持同一既有设置，不临时改变目标 |
| 逻辑决策轨迹、全量审计回调、不同读取集合诊断 | 编译期无操作策略 |
| `AnalyzeSplitOperation/ObserveStrictSplitConvergence` | 不调用 |
| `ProbeDataOrientedRoamPassWorkload`、完整输入 hash | 不在计时包络内调用 |
| `PassPolicy` | 既有串行增量策略；NMP-01 无并行规划/物化 |

冻结视图/阈值/约束不变，保留计时所需常数摘要和实际分数计算。诊断的 a/u/r/L 与时间可来自同一输入的不同遍，必须标明模式，不把开启全量审计的耗时相加到关闭诊断的结果。

全部命令通过 WSL Ubuntu。先确认可用 Linux CPU 构建工具链，保留编译器和构建类型；配置/构建使用现有 CMake 的 CPU 无窗口目标，不为本阶段运行 D3D12 或全部 CTest。若缺少关键依赖，记录环境阻塞，不混用 Windows 结果伪造同期基线。

只有具体异常才追加第二组短对照；不追究几微秒差值。共享改动使原参考出现可重复且有工程影响的退化时，记录原因/未知项并由用户决定修复小规划或后续调整。Planner 较慢单独记录，不因此自动开启性能修复支线。

## 10. 实施顺序与中途 Review

1. **字段与基线**：完成 Γ 各字段的写入点/读取者/不可导出理由；冻结出口和轨迹协议；构建中性 Legacy 基线探针。内部关系必要性无法说明时先停在此处，不把整行塞进 Γ。
2. **私有视图与堆**：实现直接借用、虚拟记录和覆盖堆，复用最小纯规则；局部夹具验证来源未变、截断/追加和计数。
3. **独立严格规划与轨迹**：实现根控制与逻辑前置，原控制器只补轻量观察；比较完整语义、J/Δ/Γ 及逻辑堆，不接生产物化。
4. **现实核查与短计量**：按固定三点诊断一次，正确后执行短计量，输出三类结论；不得因 Planner 慢重建整套基准。
5. **边界审查与回填**：审查依赖调用图、Γ/正常出口、峰值覆盖、计费与原参考开销；回填本页并出阶段实现审查，再决定 NMP-02。

| 观察结果 | 本阶段判断 |
| --- | --- |
| 语义一致、出口封闭、覆盖明显局部，Planner 较慢 | 支持解耦与当前输入的稀疏性；保守容器成本另记，可讨论 NMP-02，不要求先赢性能 |
| 语义一致，但 a/u/读覆盖/h 在自然点与压力点广泛接近全域 | 语义实现成立，局部性价值不足；按数据 Review，暂不继续铺集成设施 |
| J 相同但决策轨迹、Γ 或逻辑堆不同 | 严格语义不成立，先定位反例；不能以最终 cut 相同通过 |
| Γ/辅助载荷包含最终生产状态，或必须旧完整执行器取 J | 解耦边界失败；停止并 Review，不通过改名继续 |
| 轨迹达到容量限制或关键字段必要性尚未解决 | 证据不完整，不宣称 Pass；明确缺口及最小下一步 |

不预设一个未经依据的稀疏比例数值作为通过门槛；报告原始覆盖和各点比例，由当前固定输入上的模式作有限范围判断。不能用一个异常点证明所有输入不可能，也不能只报告最小比例点。

## 11. 结果与审查回填

实施后记录：实际文件/接口、Γ 字段依据表、首个差异及解决、三点语义结果、覆盖/规模、基线/后版本/规划费用、未完成项与阶段判断。证据放 Git 忽略的 `benchmark-output/roam-materialization/nmp-01/`；新文件写入前先核对该目录被忽略，若需补规则只增加此精确路径。

阶段审查按[规范](../../reviews/review_guideline.md)写 `docs/reviews/roam_parallelism/native_materialization_01_review.md`，检查职责、独立控制器、生产依赖、注释与规划比对。注释使用项目约定的自然中文和 `/// <summary>`，不使用 `@brief`、阶段编号或无标点挤成一行的说明。

### 11.1 初始步骤 1 的实施范围

用户要求先提交规划，再继续；已提交 `c2cd2d8`。随后按步骤 1 实现：

- `tests/DataOrientedRoamNativeMaterializationTestSupport.h`：按场景 ID 恢复三个冻结输入，测试对象共同持有高度图、流水线与入口副本。复用原生阶段观察器和冻结相机清单，不调用 MPR 桥接。
- `tests/DataOrientedRoamNativeMaterializationProbe.cpp`：中性 Legacy 基线入口，每个输入一次热身、两次计量，恢复和输入哈希位于包络外；记录来源、恢复、原完整细分阶段费用。
- `tests/DataOrientedRoamNativeTargetTests.cpp`：历史关系见证、单变量续接、孩子底边局部恢复及队列幂等边界审计，复用原生逐根步进、拓扑与队列验证器。它目前只覆盖步骤 1，不冒充完整 Planner 回归。
- 根 CMake 在无应用测试配置中准备 GLM/STB；测试 CMake 注册上述探针与审计目标。当时未修改 `src/` 生产源码；后续创建的 `materialization/` 工作区见 §11.10。

### 11.2 Γ 写入、读取及实现依据

| 字段类别 | 原写入点与后续读取者 | 当前判断 |
| --- | --- | --- |
| 本帧细分阻塞 | `BlockPersistentSplitQueueNodeForCurrentBuild` → `SplitQueueScore` | 净活动事件差分不直接表达失败处置；已实现最终阻塞轮次例外，并以 k=0/h=1 夹具核查 |
| 失败合并移除 | `RunStrictSplitStep` → `RemovePersistentMergeQueueCandidate` → 后续队首查询 | 必须结合最终成员与后续重新插入撤销，不能把每次失败永久输出 |
| 激活、合并、细分轮次与强制来源 | `PrepareSplitNodeState`、`PrepareMergedNodeState` → 分数抑制、调试类别及网格调试刷新 | 已按净转移默认值排除可恢复写入，命名例外与完整参考独立对照一致 |
| 本轮路径 | `RecordSplit` 插入；帧尾 `CollectActiveSplitPaths` 重建 | 私有逐步投影核查，帧尾重建前无额外续接后果的追加不导出 |
| 缓存创建 | `AddNode` → 后续复用及迭代上限中的节点数量 | 已实现额外休眠新缓存存在性提取，目标隐含身份不重复进入 Γ；三自然点此类均为 0 |
| 内部 `BaseNeighbor` | 连接与反向替换 → `CanMergeNode`、合并资格和邻域收集 | 活动内部 Base 的固定伙伴几何核查通过；采用目标固定伙伴恢复协议，实际物化见后续 NMP-02 |
| 内部 `LeftNeighbor/RightNeighbor` | `LinkSplitNeighbors/ReplaceNeighborReference` 留存或更新 → `AppendPersistentMergeQueueNeighborhood`；合并时从孩子底边恢复 | 8/9 及深层规范化续接均未发现必须保留历史值的证据；首版不准入关系 Γ，按 §11.9 恢复协议实施 |
| 休眠孩子关系 | 复用时 `PrepareSplitNodeState` 清空三邻接；此前的邻域收集已可能读取它们 | 124 个缓存清空关系后重新激活的续接一致；不能称为无人读取，采用 §11.9 的条件化恢复协议 |

这里保留字段审计依据；实际 schema 1 及 h 计量已实现，见 §11.11/11.12。没有将完整内部关系列入实际 Γ 类型。

### 11.3 局部见证及结论边界

从两个根构造深度 3 的合法一致细分，固定 H=6、B=4096。解析夹具使用人工分数及 1e30/-1e30 的细分/合并阈值来指定请求次序，并恢复堆和反向索引；它不代表自然视点的评分输入。枚举 16 个叶请求的 120 个无序二元组，分别沿原生逐根路径应用 A→B 与 B→A；已经被强制完成的第二根不重复执行。

每个最终状态检查共形拓扑、叶邻接、无 T 接缝、完整队列不变量和预算。120 对最终叶集合相同；内部关系差异共 72 处，其中 Base 为 0、Left 为 36、Right 为 36；48 处后续合并邻域集合不同。

首个见证：请求 8 与 9，内部节点 8 的左邻接分别为 9 与 19；内部节点 9 的右邻接分别为 16 与 8。对内部节点 9 调用原生邻域收集时，B→A 的结果多含路径 11。

**此处使用人工优先级构造不同合法历史；不是同一完整已评分 M₀ 下严格控制器产生不同结果的反例。** 它只说明最终叶集合不能唯一决定原生历史关系，且这类关系确有后续读取。它没有证明：这些关系不能由 M₀+Δ 局部恢复、必须携带于 Γ、已经改变严格决策，或目标决策与物化无法解耦。

初次审计依 §10 步骤 1 的“内部关系必要性无法说明时先停在此处”进入中途 Review。用户随后确认继续隔离历史轮次、叶邻接和队列等变量，结果见 §11.7。不能因已有白名单列出 Base/Left/Right 就自动认为整组字段均获出口许可。

### 11.4 中性基线与验证记录

Ubuntu / GNU C++ 15.2.0 / Release / Ninja；无窗口 CPU 构建目录 `/tmp/workload-roam-nmp01-build`。冻结相机复用 `benchmark-output/roam-materialization/mpr-01/input-freeze/inputs/camera-samples.csv`；资产由现有清单读取器核对。有效基线为 `benchmark-output/roam-materialization/nmp-01/legacy-before-v2.csv`，对应冻结可执行文件 `legacy-before-v2-probe`。

| 输入 sample14 | 缓存 N | Qs / Qm | 入口叶数 → 结果叶数 | 两次原细分阶段墙钟（ms） |
| --- | ---: | ---: | ---: | ---: |
| test129 / B4096 | 8,332 | 4,096 / 710 | 4,096 → 4,096 | 0.059242、0.108283 |
| Peking / B20000 | 40,936 | 19,990 / 3,347 | 19,990 → 20,000 | 1.889717、1.481537 |
| Peking / budget-orbit64 / B200000 | 745,072 | 195,367 / 32,856 | 195,367 → 200,000 | 61.711479、86.244003 |

这是一组开发基线，不宣称稳定均值、尾部或跨 Windows/Linux 加速。两次重复存在波动；初次记录时没有生产代码改动，不为收紧波动继续追加计量。后续工作区进入共同编译清单后的对照见 §11.10。

首次探针错误地按清单返回位置选择场景，清单排序导致压力项使用 test129 参数。已改为按 ID 查找；`legacy-before.csv` 与旧 `legacy-before-probe` 为无效压力协议记录，不能使用，保留在忽略目录说明来源错误。有效 v2 二进制与 `SourceSupport-v2.h` 单独冻结，没有覆盖旧结果。

定向 CTest `DataOrientedRoamNativeTarget` 完成；`relation-audit-validated.txt` 保存逐例摘要，测试职责拆分后的最终记录为 `relation-audit-final-ctest.txt`。初次审计时 Legacy 探针与冻结 v2 二进制通过 `cmp` 一致，故沿用有效基线，没有追加相同二进制的性能对照。没有运行其余阶段、MPR 全矩阵、全部 CTest 或图形后端构建。完整规划器、独立决策轨迹、自然 a/u/r/L/h、Tplan 及物化费用仍未实现或测量，不能填零或写成通过。

以下为 Ubuntu 内复现命令；探针拒绝覆盖结果，重跑时使用新的输出文件名：

```bash
cmake -S /mnt/d/CPP-Projects/WorkloadAwareROAM -B /tmp/workload-roam-nmp01-build -G Ninja -DCMAKE_BUILD_TYPE=Release -DPARALLEL_ROAM_BUILD_APP=OFF -DPARALLEL_ROAM_BUILD_TESTS=ON
cmake --build /tmp/workload-roam-nmp01-build --target parallel_roam_DataOrientedRoamNativeTarget_tests parallel_roam_native_materialization_probe -j 4
ctest --test-dir /tmp/workload-roam-nmp01-build -R '^DataOrientedRoamNativeTarget$' --output-on-failure
/tmp/workload-roam-nmp01-build/tests/parallel_roam_native_materialization_probe /mnt/d/CPP-Projects/WorkloadAwareROAM /mnt/d/CPP-Projects/WorkloadAwareROAM/benchmark-output/roam-materialization/mpr-01/input-freeze/inputs/camera-samples.csv /mnt/d/CPP-Projects/WorkloadAwareROAM/benchmark-output/roam-materialization/nmp-01/legacy-reproduction.csv
```

### 11.5 阶段出口

最初在 8/9 见证后暂停字段审查；用户随后确认继续收敛剩余规则。深层/休眠结果及工作区协议见 §11.8/11.9，步骤 2 实现见 §11.10；用户要求直接完成剩余 01 后，步骤 3～5 的实现与最终判定见 §11.12。原生物化与完整恢复证据仍属于 NMP-02。实现及[阶段架构审查](../../reviews/roam_parallelism/native_materialization_01_review.md)暂未提交，此前 Git 授权已用于规划。

### 11.6 内部关系隔离对照（用户已确认继续）

用户认可“只替换内部左右邻接，其余完整状态相同，再执行相同续接”的下一步。本轮 Extend `DataOrientedRoamNativeTargetTests.cpp`，在同一测试目标中加入隔离、逐步对照和字段读取依据；复用既有状态快照、原生步进及根操作观察，不改变生产职责或添加新的控制器。

冻结使用首个 8/9 见证：以 A→B 状态为完整来源，另一合法历史仅提供内部左右邻接的替代值；按稳定身份映射回来源物理下标。通过替换、还原和完整存储快照核对，确保除此之外没有修改任何来源字段。后续两边使用相同配置、队列与实际原生控制器，不分别沿不同历史继续。

最小续接集合为：下一轮细化、下一轮合并、硬预算交换、仅一份空余预算、同轮预算阻塞。每个路径逐轮比较根/合并选择与结果、停止与预算、完整逻辑队列、叶/内部事件、缓存和历史字段；调用既有 `AnalyzeSplitOperation` 只在这个微型诊断中补充强制请求与预算账本，不用于生产规划、时间计量或自然大状态逐根哈希。既有观察没有 `forcedFrom` 字段，本轮不称为 §6 的完整新旧决策轨迹验收。

同步检查所有内部左右邻接读取点，并分析“邻域额外访问幂等”所需的完整队列、当前分数和资格复核条件。未观察到差异不自动证明可删除字段；发现差异也须先隔离到逻辑结果，不能只看堆形状或维护次数。失败合并处置的分析必须区分合法新鲜队列与人工缺项/过期项，不能通过破坏队列不变量制造自然反例。

对幂等条件补一个同状态邻域失效/刷新往返检查，并用显式人工缺项展示重新插入的边界；后者单列为组件诊断。局部恢复仅针对 8/9 见证中孩子仍为活动叶的父节点，按 `Left=Base(leftChild)`、`Right=Base(rightChild)` 重新计算，再沿同五种配置续接；不直接推广到任意深层内部或休眠缓存字段。

生产和基线探针源码不变时，沿用已冻结基线及二进制一致性证据，不追加性能矩阵。结果、覆盖到的分支和剩余边界回填本节及中途审查。

### 11.7 隔离与恢复结果

Ubuntu Release 下定向测试可执行文件通过，记录为 `benchmark-output/roam-materialization/nmp-01/relation-continuation-audit.txt`。没有增加自然输入或性能采样。五个配置都从同一个 A→B 完整来源独立恢复，历史替换只改变节点 8 的 Left 与节点 9 的 Right；换轮、分数刷新及预算设置在复制前对共同来源完成。

| 续接配置 | 严格循环步数 | 根 split 尝试 | forced 尝试 | 成功合并根 | 预算交换 | 含预算拒绝的根 | 停止 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 下一轮细化 | 67 | 66 | 43 | 0 | 0 | 0 | NoEligibleSplit |
| 下一轮粗化 | 12 | 0 | 0 | 11 | 0 | 0 | NoEligibleSplit |
| 无空余预算、允许交换 | 4 | 4 | 3 | 2 | 2 | 3 | BudgetBlocked |
| 仅一份空余预算 | 4 | 4 | 5 | 2 | 2 | 3 | BudgetBlocked |
| 同轮预算阻塞 | 1 | 1 | 0 | 0 | 0 | 1 | BudgetBlocked |

合并根计数不是 primitive 合并三角形数；forced 计数包含尝试，不等于全部成功。每步比较根/合并选择与成败、强制尝试序列与结果、逐项预算余量、阻塞与停止、完整逻辑堆、活动成员、缓存身份/创建顺序、Base、轮次和分数缓存。两端拓扑/队列/预算不变量均保持。内部左右邻接是唯一允许保留的关系差异，节点一旦成为叶就重新要求左右邻接相等。

第二组使用同一见证父节点，但干预值由目标孩子直接计算：`Left=Base(leftChild)`、`Right=Base(rightChild)`。先要求两个孩子均为活动叶；另一个历史只标定待审计父节点，不提供恢复值。相对 A→B，重算后一个字段变化，另一个本来就与该规则一致；其五条完整续接轨迹及上表计数仍相同。规则对已定位父节点是常数次读取；测试为定位和完整对照所做的全池枚举不属于已经实现的生产 O(k) 算法。

**解释与条件：**

1. `CanMergeNode/EvaluateMergeCandidate` 与 `IsMergeableTopology` 读取父子、活动、Base 和评分，不读取内部 Left/Right。`ReplaceNeighborReference` 的三个字段更新各自独立。真正合并时先从孩子 Base 覆盖父 Left/Right，再使父恢复为叶；真正细分使用的是当前叶的左右邻接。
2. `AppendPersistentMergeQueueNeighborhood` 的额外成员仍可改变维护工作和物理堆形状。对拓扑、轮次、视图不变，且成员完整、分数有效的逻辑 Qm，额外失效/重新插入可合并代表会恢复同一成员/伙伴/分数。对 8/9 邻域各做一次无拓扑变化的往返，实际发生 8 次插入，逻辑投影保持不变。
3. 这只是“额外刷新未变化候选”的幂等论证，不能单独证明任意规范化邻域都覆盖每个受影响候选。完整队列既是前提，也是逐步核查项，不能靠假设结果完整来宣布所有后续状态正确。
4. 人工从完整 Qm 删除一个仍合法的候选，独立验证器检出缺项；仅刷新该候选就将其补回。这说明缺项时刷新不再对逻辑队列幂等。该组件夹具不是自然失败合并反例，不能据此构造 Γ 必要性。
5. 严格细分循环的实际合并使用最大有限分数上限。在完整拓扑候选和有限重算分数的前提下，资格条件足以通过该合并复核；本组每步额外核查现有 Qm 均可通过此复核，实际失败合并为 0。不声称覆盖了失效候选、非有限数值或其他阶段的有限阈值失败。

**字段结论：** 8/9 见证支持将“被读取的历史值”和“必须保留的续接义务”分开，内部左右邻接没有取得 Γ 准入依据，关系差异本身不再阻断这一见证的局部恢复。当时未覆盖的深层内部和休眠情形已继续在 §11.8/11.9 定向处理；网格日志消费、新旧规划器完整轨迹及正常出口 schema 仍未验证，不通过整行关系快照绕过这些边界。

### 11.8 深层内部与休眠缓存的定向收敛

用户已确认继续完成剩余字段规则并进入私有视图/堆。本轮仍在既有目标测试中扩展一条固定深度轨迹：H=6，从深度 3 一致细分推进到深度 5，再进行下一轮粗化和下一轮细化；不是新增自然性能输入。

诊断规则分两类：活动内部节点暂按两个孩子的 Base 重算 Left/Right，允许孩子本身为内部节点；休眠节点清除三个旧邻接。活动叶关系与活动内部 Base 不变。每次干预以完整快照验证只有声明的关系字段改变，之后从该状态直接继续原生严格循环，不全量重建队列或重导入。逐步逻辑投影只对明确被干预的非活动关系放宽比较，重新激活时立即恢复活动关系核对；原先 8/9 检查仍采用更严格的原比较。

该轨迹重点检查：实际存在深层内部、实际清除非空休眠关系、实际复用旧缓存，且逐步活动拓扑、Base、资格、完整逻辑队列与决策保持一致。有限测试配合读写条件说明恢复依据，不提升为所有状态的形式化证明。若发现漏候选，定位必要邻域，而不是重新导出整行状态。字段语义收敛后按 §3 创建视图/堆，生产拓扑派发继续留在后续阶段。

### 11.9 工作区实现采用的字段协议

深层轨迹通过：30 个深层内部节点参与规范化，48 个字段改变；粗化到两个根后，124 个休眠节点的 320 个非空关系被清除；再细化复用了全部 124 个旧缓存身份。两段分别为 38/76 个严格循环步，37 个合并根、75 个细分根与 51 个强制尝试，决策、逻辑队列及活动投影一致。所有字段夹具还按域的底边反射独立核查活动内部 Base：边界为空，内部边为同层互指且已细分的伙伴。

针对当前已认证共形来源，采用以下**恢复协议与待验收承诺**，据此推进工作区；它不是新增全状态快照，也不是所有原生状态的形式化证明：

- 活动叶三邻接由目标叶边恢复；活动内部 Base 由固定同层菱形恢复；内部 Left/Right 可按两个目标孩子的 Base 规范化；非活动缓存三邻接可清空。恢复涉及差分节点及关系改变孩子的父节点，不要求每次扫描/规范化整个旧缓存池。真实局部实现及切回 Legacy 的续接仍由 NMP-02 验证。
- 资格条件只依赖活动、孩子、Base 和当前分数。一次 primitive 改变的活动节点及其父/底边伙伴由操作两侧的前后邻域覆盖；历史内部左右关系和休眠关系扩大的是可冗余刷新集合。该覆盖说明只针对当前 split/merge 写集合和完整、有效 Qm，不允许将其推广为任意裁减邻域均正确。
- 规划期间**不使用上述规范化替代原逻辑关系演化**。稀疏视图仍精确模拟需要读取的三邻接，保证动态选择审计独立于未来物化恢复。正常出口首版不给任何关系字段 Γ 准入；若后续出现合法反例，停在具体字段和恢复条件 Review，不自动填充 `InternalRelationChange`。
- Γ 保留已命名的本帧阻塞/最终失败移除、非默认轮次/强制激活、额外缓存存在性。每个最终值与 M₀+Δ 的恢复默认值比较，等值删除；同键只保留最终值，候选重新插入撤销失败处置。默认新缓存创建轮次为当前构建号，父子/域/深度从身份推导，目标已隐含的缓存身份不重复登记。
- `CurrentSplitPaths` 在控制器中只追加，决策读取的是 `PreviousSplitPaths`，帧尾在使用前重新收集最终活动事件。因此私有视图可精确记录本轮追加供审计，但不将整个路径集合或无续接后果的追加写入 Γ。根/primitive 成败计数进入摘要与轨迹，不伪装成状态义务。

步骤 2 的具体文件继续按 §3：`NativeMaterializationTypes.h` 先定义实际使用的工作区字段/计数；`NativePlanningView.*` 管理逐字段覆盖、虚拟孩子与预算；`NativePlanningQueues.*` 管理两堆槽位、反向位置及菱形代表覆盖；`DataOrientedRoamIndexedHeap.h` 只定义静态堆修复原语。Legacy 暂不改用新 helper，复用接线与轻量观察在步骤 3 一起核对旧路径成本。`NativeTargetPlan` 的实际提取/规范化随控制器实现，不提前创建无行为空壳。

工作区以 `std::map` 保留曾写槽位/字段记录，恢复基态值或截断尾部不消除累计覆盖证据；未覆盖读取直接借用 M₀。诊断模式另记录真正回读基态的不同节点/槽位，普通模式不维护这些集合。读写次数、覆盖记录与反向成员分别计数，后续报告不将记录载荷字节称为含分配器的实测总内存。

测试按职责细分：Create `tests/DataOrientedRoamNativePlanningTests.cpp` 专门验证工作区只读借用、虚拟节点、堆覆盖/反向关系与计数；原 `NativeTargetTests.cpp` 保留字段续接审计，避免继续混入独立容器测试。两者使用相同现有无窗口构建模板。组件测试可以输入已明确标注的抽象成员/分数来审计堆，不将这些改组冒充合法拓扑规划。新增源码注册到共享 DOD 清单，生产尚无派发入口。

### 11.10 稀疏工作区组件实现与验收

实际新增六个核心文件及一个组件测试文件：

| 文件/接口 | 已实现内容 |
| --- | --- |
| `materialization/NativeMaterializationTypes.h` | 私有字段、活动资格、节点/堆计数值类型；尚不定义正常 `NativeTargetPlan` 输出 |
| `NativePlanningView.h/.cpp` | `const M₀` 借用、逐字段 `optional` 覆盖、虚拟孩子、两根路径链查找、预算预留/释放和触及记录 |
| `NativePlanningQueues.h/.cpp` | 两堆长度/来源可见前缀、槽位与反向位置覆盖、平分身份排序、任意删除/更新、菱形代表改组 |
| `DataOrientedRoamIndexedHeap.h` | 无持久状态的静态 sift/restore 原语，访问与交换由调用者绑定；当前 Legacy 尚不调用它 |
| `tests/DataOrientedRoamNativePlanningTests.cpp` | 构造不展开来源、源完整快照不变、覆盖还原、虚拟身份/误差、预算、堆尾截断/重用及伙伴移除/改组 |

字段覆盖记录和曾写堆槽位保留到工作区销毁。因此当前“累计覆盖”“记录峰值”“分配记录末态数量”相同；`ChangedFields` 单独表示净覆盖字段，不能用它替代 a。堆旧/追加槽位以入口长度划分；反向记录及合并关系记录分列。诊断使用独立集合记录真正回读来源的不同节点/槽位，普通模式不创建这些集合条目。`Metrics()` 只遍历私有记录，费用是 O(a+u)，没有按 N/Q 建立全量身份或成员表。

`CreateChildren` 复用二分域、稳定路径及 variance lookup，不调用生产 `AddNode`。域、深度、误差与关系覆盖属于决定后续目标所需的工作区，生命周期与同步调用一致；没有活动稠密数组、mesh、物理槽位或目标堆导出。虚拟孩子初始为休眠，创建和成功细分分开；实际严格控制器负责后续激活及候选资格。

**正确性与规范核对：**

- Ubuntu Release 构建通过。定向 CTest `DataOrientedRoamNativeTarget`、`DataOrientedRoamNativePlanning` 均通过，总测试墙钟约 0.05 s；原始记录为 `deep-dormant-audit.txt` 与 `storage-tests.txt`。
- 组件分别在诊断关闭/开启下与同一独立成员集合比较，源快照含长度、容量和地址。测试实际覆盖清空后重新追加、追加越过来源长度、恢复原值仍保留累计覆盖、从伙伴移除和解除两个旧菱形后改组。
- 合并堆组件不代替资格检查；任意抽象改组只验证存储语义。尚无新旧完整目标或控制器比较。
- 未安装 Ubuntu `pwsh`；使用忽略目录的只读 Python 按现有 PowerShell 脚本相同行规则核对受影响门禁：`src` 为 3110/20240=15.366%，DOD 为 1135/5654=20.074%。人工补充检查新增注释的语义、摘要格式和三行正文约束，没有脚本替换源码。

**原参考短对照：** 新文件进入共享构建清单，原拓扑/评分/队列实现未修改，计时入口不调用新工作区。为检查重新链接后的原参考，使用同一探针和冻结三个输入执行一次热身、两次计量；输入 hash、N、Qs/Qm、入口/结果叶数与 v2 基线一致。

| 输入 | 修改前均值 ms | 后版本首测 ms | 冻结旧二进制复测 ms | 后版本复测 ms |
| --- | ---: | ---: | ---: | ---: |
| test129 / B4096 | 0.083762 | 0.124083 / 0.173304 | 0.076092 / 0.118273 | 0.098773 / 0.348638 |
| Peking / B20000 | 1.685627 | 1.674682 / 1.812076 | 1.323083 / 1.567299 | 1.565299 / 1.802504 |
| Peking / B200000 压力 | 73.977741 | 69.274582 / 77.488815 | 61.649376 / 58.278522 | 62.026173 / 60.901890 |

首测 test129 均值增加 0.064931 ms，超过基于旧数据的 0.05 ms 筛查线；因此仅追加一次旧/后版本对照。既有冻结探针一次包含三个输入，为避免另写计量实现，两次复测均沿用此入口，没有新增线程或自然轨迹。Peking 两点变化均在原波动范围内。test129 后版本复测包含 0.348638 ms 离群值，均值仍偏高；不能据此宣布性能无回归，也不能用一个重叠样本证明版本差异不存在。

当前只能确认计时入口没有执行新容器，偏移不是已测得的 Planner 工作量开销；调度、缓存/链接布局等原因尚未分离，样本不足以判定稳定版本因果。按开发规范只保留风险与记录，不再追加微秒级追测、不自行开启性能修复，也不宣称问题已修复。三份原始文件为 `legacy-after-storage.csv`、`legacy-control-storage.csv`、`legacy-recheck-storage.csv`。独立进程仍是统计单位；本表均值只是开发摘要。

**当时出口：** 步骤 2 的存储组件成立；当时尚无独立控制器、完整轨迹、净出口或 Tplan，故未提前标记整阶段通过。后续步骤 3～5 已完成，最新结果见 §11.12。没有接入 NMP-02、原／新生产开关或多线程物化。

### 11.11 严格控制器与现实测量的实施契约

用户要求本轮直接完成 01，再统一汇报。继续按步骤 3～5 实现，不提交 Git，不接入 02。`NativeRefinementSimulation` 负责逻辑 primitive 与局部候选演化，`NativeTargetPlanner` 负责独立严格循环与净出口；二者不调用 Legacy controller/mutator。共享评分增加域/误差描述与路径迟滞入口；轻量 Legacy 决策观察采用编译期策略，普通路径不存 trace。停止枚举移到纯决策值头，原实验 API 保留原名称。

测试职责细分为 `DataOrientedRoamNativePlannerTests.cpp` 与 `DataOrientedRoamNativePlannerTestSupport.h`：前者管理解析控制夹具，后者持有外部完整投影、独立 Γ 提取和有界轨迹，供测试与现有探针复用。没有生产反向依赖。完整投影遍历整个参考缓存/队列，不把 Planner 宣称触及的集合当参考输入；微型夹具逐循环比较，自然点比较全部轨迹和完整终态。

正常结果 schema 1 仅包含净事件、以下具名义务、同步环境/预算/停止摘要及纯分数缓存。默认历史从 M₀ 与净活动转移恢复：新增内部事件默认 SplitBuild 为当前轮，移除内部事件默认 MergeBuild/ActivatedBuild 为当前轮且 forced=false；休眠变活动默认 ActivatedBuild 为当前轮且 forced=false；其余默认保留旧值，新缓存默认创建/激活轮次为当前轮。偏离默认值的激活、细分、合并、forced 和阻塞字段才进入 Γ。最终目标已经隐含的缓存不重复登记，额外休眠新缓存登记存在性；失败移除只有最终仍应作为规范合并候选、但被抑制时才有持续义务。每键只读取最终值，无完整关系或 heap 载荷。

当前入口明确支持 §9.2 冻结的 `MirrorSplitScoresToNodePool=false`、共形约束开启且预算合法的来源；其他配置拒绝，不能悄悄改输入得到不同问题。GUI 镜像字段和新旧生产切换不在本轮范围，须在 02 正式接线前补齐或明确约束，不能把本轮范围称为所有生产设置已兼容。源码中来源历史的出口读取也经过 View 计费入口，普通模式不建立不同读集合；诊断读集合是额外费用，最终全量投影在计数冻结后执行。

**自然运行前冻结：** 仍只使用 §8.2 三个 sample14 输入。现有探针增加 `--planner` 模式，分别输出 schema 2 计时文件与同名 `.audit.csv` 诊断文件，拒绝覆盖。每输入一次完整 Legacy/Planner 诊断，诊断通过后各一次热身、两次计量；无逐根副本或足迹哈希。诊断开/关分别比较结果，Tplan 含工作区创建、计算、提取和销毁，返回结果清理另记；完整来源恢复与全量审计在包络外。只执行这一组新控制器短测，不为 §11.10 旧计时风险追加复测。预计分钟级，逐输入输出进度，任何语义不符先停止该测量；不扩展场景/线程/重复数量。

### 11.12 完整严格规划器与阶段验收结果

#### 实现与边界

已实现 `NativeRefinementSimulation.h/.cpp`、`NativeTargetPlanner.h/.cpp` 和 `DataOrientedRoamDecisionTrace.h`。`BuildNativeSplitTarget` 从只读 M₀ 独立执行低分合并、严格细分头部、强制前置、根失败后的新队首查询、预算交换、阻塞及停止；不调用旧完整执行器、旧 primitive、MPR Bridge、mesh writer 或完整输入哈希。正常返回 `NativeTargetPlan` schema 1，工作区在返回前析构。结果只含净事件、封闭 Γ、纯分数缓存和标量摘要/计数，无目标关系、活动数组、物理堆或槽位。

Legacy 的 `SplitNodeImpl` 和严格循环只增加编译期决策观察，普通实例不发事件，不收集访问足迹；控制分支与 mutator 保留。新旧共享域/误差评分和路径迟滞规则，停止枚举保持原数值并移入纯值头。旧堆暂未改用新 `IndexedHeap`，资格条件在独立模拟侧按原语义实现并对照；不为共用抽象再改原热容器。这样保留独立决策证据，没有把 Legacy 包装为 PlanningView controller。

精确关系仍在私有模拟中演化，§11.9 的关系规范化仅是后续物化协议。Γ 实际类型只允许阻塞、最终失败候选抑制、非默认激活/细分/合并轮次、forced 标记、额外缓存存在性。没有 `InternalRelationChange` 实际载荷类型。`Initial()` 为出口默认值读取计费；返回结果中的来源轮次/规模只描述同步事务，不是可跨任意状态使用的认证令牌。

轻量轨迹编译期关闭；不同读集合和外部审计采用关闭的布尔/空回调，不分配诊断集合，但保留普通标量计数和少量开关检查。这里不声称所有观察分支都在汇编中消失。`r` 的诊断值含轨迹获取稳定身份所需查询；完整终态投影在计数冻结后执行，不混入报告覆盖。常规计时不运行轨迹、集合诊断或全量回调。

#### 正确性结果

11 个解析配置通过逐循环完整逻辑投影、完整决策事件、独立 Δ/Γ 和诊断开关对照。包含细化至最大深度、粗化、复用缓存、0/1/2 空余预算、平分/迟滞停止、合并 `<` 边界和同轮预算抑制。两项故障注入分别覆盖失败合并与非叶前置失败，明确不是自然合法输入；后者实际得到 **k=0、h=1**，说明净目标不变仍可能有具名阻塞义务。预算加 2 的合法配置实际出现一次“根失败但成功前置保留”。另定向核查原迭代上限的后增边界，不冒充自然上限耗尽。

首次解析失败来自测试把全部 Qm 分数设成同值却没有恢复平分 Path 堆序；已修正夹具的堆和反向位置，没有修改生产语义迎合该输入。构建时补齐测试直接依赖头和实际验证器名称。没有发现需要改变算法边界的语义反例。

| 冻结输入 | 完整决策事件 | 最终叶数 | Added / Removed | k | h | g | 严格结果 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| test129 / B4096 | 69 | 4096 | 6 / 6 | 12 | 6 | 12 | 一致，BudgetBlocked |
| Peking / B20000 | 413 | 20000 | 44 / 34 | 78 | 44 | 84 | 一致，BudgetBlocked |
| Peking / B200000 压力 | 156720 | 200000 | 16208 / 11575 | 27783 | 16610 | 25290 | 一致，BudgetBlocked |

三点均逐事件比较完毕，未触及一百万事件上限；外部独立投影比较全部缓存身份/域/误差/关系/活动/历史、全部逻辑堆成员/分数/伙伴、预算和停止位置。来源完整快照包含容量和地址，规划前后不变。自然输入 Legacy 开/关轨迹的完整终态一致，Planner 开/关诊断的 Δ/Γ/求值缓存/工作摘要一致。自然 Γ 全部为 forced 激活例外，其他六类均为 0；不能据此删除尚未在自然点发生的防御义务。

本阶段仍没有原生物化，不能声称这些义务已由生产 materializer 恢复或已通过新旧模式切换。评分镜像开启的生产设置仍在当前入口支持范围外。

#### 局部性与实际工作

| 输入 | a_old / N | 旧节点读取覆盖 | u_s_old / Qs | u_m_old / Qm | Qs 读取覆盖 | Qm 读取覆盖 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| test129 | 44 / 8332 = 0.528% | 1.632% | 75 / 4096 = 1.831% | 21 / 710 = 2.958% | 3.271% | 5.070% |
| Peking B20000 | 256 / 40936 = 0.625% | 1.813% | 454 / 19990 = 2.271% | 195 / 3347 = 5.826% | 4.142% | 9.979% |
| 压力 B200000 | 62537 / 745072 = 8.393% | 13.598% | 73852 / 195367 = 37.802% | 18923 / 32856 = 57.594% | 55.783% | 76.035% |

节点覆盖是局部的，h 分别约为 0.50k、0.56k、0.60k；**压力点不能称为整个 heap overlay 仍非常稀疏**。节点记录与堆槽位具有不同尺度，不能以 8.393% 的节点覆盖掩盖 76.035% 的合并堆读覆盖。构造未扫描全域，不意味着严格 heap 演化不会覆盖大部分已有槽位。

压力点实际记录 r=7,037,760、堆槽位读取 Ls=2,009,662/Lm=955,354、堆写入 702,056/326,942、成员读取/写入 821,276/1,354,913；纯评分 85,900 次，邻域访问 1,795,522 次，候选检查 428,207 次。累计旧/虚拟覆盖记录为 62,537/25,290，新增堆槽位为 4,633/1,052，反向覆盖为 117,843/37,495，代表关系记录 65,194。不能把 g 与 a_old 混算成旧池覆盖比例。

返回载荷字节为 784、5,040、1,447,576；求值缓存分别 34、210、51,667 项。这是 vector 元素逻辑载荷，不含容量预留、工作区 map/set 节点、分配器及测试投影，不称为实测总内存。字段/堆覆盖保留到返回前，累计/存量/峰值记录数相同；Γ 直接从最终值形成，没有单独中间历史日志，因此 hPeak=h。一般内存峰值和所有输入的 O(k) 界未获证明。

#### 短计量与性能边界

原始文件为 `planner-final-v1.csv` 和 `planner-final-v1.csv.audit.csv`，来源 hash、N、Qs/Qm 和入口/最终叶数均与冻结 v2 一致。仍是一次独立进程、每方法一次热身加两次计量，非正式统计矩阵；先诊断后计时，每次 Legacy 恢复在计时外，Planner 直接借用同一来源。

| 输入 | LegacyAfter 两次 ms | Tplan 两次 ms | 均值 Legacy / Plan ms | Plan / Legacy 费用比 |
| --- | ---: | ---: | ---: | ---: |
| test129 | 0.040741 / 0.051282 | 0.108133 / 0.095992 | 0.046012 / 0.102063 | 2.22 |
| Peking B20000 | 0.512813 / 0.469231 | 1.054026 / 0.936874 | 0.491022 / 0.995450 | 2.03 |
| 压力 B200000 | 64.335556 / 58.923840 | 1105.827881 / 1071.302490 | 61.629698 / 1088.565186 | 17.66 |

结果所有权清理为：test129 0.000050/0.000070 ms、Peking 0.000100/0.000090 ms、压力 0.001430/0.001820 ms；单列原值，不围绕微秒差异展开结论。Tplan 已含工作区构造、控制、求值、净出口提取和私有容器销毁，尚不包含原生物化。

这些费用比不是加速比：规划与旧完整提交输出不同，未来还需要加上 Tmaterialize。当前压力点规划本身已远贵于旧完整细分阶段，直接接入此容器版本不能产生完整收益。可确认的成本来源包括多级有序覆盖查询、heap sift 中重复槽位/成员维护、节点字段覆盖、求值缓存和出口整理；源码及计数支持这些工作确实发生，但本轮没有分阶段 profiler，不能给出它们各自占 1.09 秒的精确比例，也不能证明换一个容器必然消除全部差距。

本轮 Legacy 未显示相对冻结基线的新增明显退化。与旧短测相比部分数字更低，但运行顺序、缓存和有限样本不足以声称共享提取带来优化；§11.10 test129 历史偏移的原因仍未知，没有用本轮更低数值追认为问题已修复。未额外复测或擅自开启性能修复。

#### 审查、验证与出口

实际执行受影响的 NativeTarget、NativePlanner 和旧 SplitPrefix 定向测试；均通过。补充 k=0/h>0、部分成功前置和交换计数断言后，仅重跑 NativePlanner；未跑全量 CTest、MPR、后端或完整自然矩阵。最终构建通过，只有既有 GLM 与 FormalCpuPairCsv 警告。新增注释逐段检查，按已有工具同一口径核对 src=15.486%、DOD=20.048%；Ubuntu 未运行不存在的 pwsh，也未用脚本替换源注释。自然计量后只补注释、禁止工作区/观察作用域复制及测试断言，没有修改被计量的算法控制或容器操作，不追加性能重跑。

复现命令（Ubuntu，输出必须为新文件名）：

```bash
cmake --build /tmp/workload-roam-nmp01-build --target parallel_roam_DataOrientedRoamNativePlanner_tests parallel_roam_native_materialization_probe -j 4
ctest --test-dir /tmp/workload-roam-nmp01-build -R DataOrientedRoamNativePlanner --output-on-failure
/tmp/workload-roam-nmp01-build/tests/parallel_roam_native_materialization_probe /mnt/d/CPP-Projects/WorkloadAwareROAM /mnt/d/CPP-Projects/WorkloadAwareROAM/benchmark-output/roam-materialization/mpr-01/input-freeze/inputs/camera-samples.csv /mnt/d/CPP-Projects/WorkloadAwareROAM/benchmark-output/roam-materialization/nmp-01/planner-reproduction.csv --planner
```

最终分项判定：

1. **严格语义解耦：本轮范围通过。** 独立稀疏规划能产生相同严格决策、J/Δ/Γ、逻辑堆和预算，来源未修改，正常出口未偷带目标生产状态。
2. **局部性：节点/义务有正面证据，压力 heap 是明确限制。** 没有全状态复制/初始化扫描，但不能宣称 u≪Q 在压力点也成立，不能给整个工作区一项无保留的稀疏性 Pass。
3. **成本：当前实现不具备生产替代收益。** 大输入规划费用很高，不能以 MPR 的物化收益抵消或隐藏。它不推翻已验证的解耦语义，是否优先优化覆盖表示或进入 02，需要基于此数据 Review，不在本轮自制容器或扩大架构。

**NMP-01 实施和验收已完成，NMP-02 未启动。** 保留 MPR 独立 oracle，原生物化、镜像配置支持、mesh 前缀消费、新旧开关和真实生产续接仍明确属于后续阶段。代码和阶段审查未提交 Git。
